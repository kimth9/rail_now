# 업무진행 상황록 (history)

> 담당자(사람·AI 세션)가 작업을 마칠 때마다 **무엇을 했는지 3~5줄 내외**로 기록하는 시간순 상황록.
> 규칙: 항상 **문서 맨 아래에 추가**(오래된 → 최신), 과거 항목은 수정하지 않는다.
> 권장 구성: 한 일 / 발견한 문제 / 다음에 할 일

---

## 2026-08-25 — Claude (프로젝트 방향 수립)

- Android 16 `Notification.ProgressStyle` 조사. 진행축=열차 위치→사용자 역, `Segment`=역간, `Point`=중간역으로 매핑 가능함을 확인. Live Update 승격 요건(`POST_PROMOTED_NOTIFICATIONS`, `setRequestPromotedOngoing`, ongoing, RemoteViews·colorized 금지)도 정리.
- 발견한 문제: 플랫폼 가이드가 Live Update를 "진행 중·사용자 시작·시간 민감" 활동으로 제한하고, 주변 정보(ambient)와 상시 접근 용도를 명시적으로 부적절 사례로 든다. 상시 전광판형 알림은 정책 위반이라 **사용자가 추적을 시작하는 세션 모델**로 설계를 틀었다.
- 커버리지를 광역전철 5개 권역(수도권·부산·대구·대전·광주)으로 잡고 데이터 소스 조사. 실시간 API는 사실상 수도권에만 존재하고 지방 4개 도시는 시각표 기반뿐임을 확인(동종 앱 「열차어디」의 지원 노선 안내로 교차 확인).
- 다음에 할 일: 시각표 기반 구간과 실시간 구간을 하나의 화면 구조로 처리하는 2계층 모델 설계.

## 2026-08-25 — Claude (기존 프로젝트 자산 조사)

- 사용자 제공 `japanese_style_timetable` 프로젝트 확인. TAGO `GetStrtpntAlocFndTrainInfo`로 OD 기반 열차 목록을 받고, **정차역 상세는 rail.blue 개인 사이트의 내부 AJAX 엔드포인트를 직접 호출**하는 구조였음을 파악.
- 발견한 문제 (1): rail.blue 의존은 배포 앱에 그대로 옮길 수 없다. 프로젝트 자체 크롤링 노트에도 "공식 API 우선 검토" 원칙이 적혀 있어, 공식 소스로 대체하고 rail.blue는 검증용 대조로만 쓰기로 결정.
- 발견한 문제 (2): 업로드된 zip에 `.env`가 실제 키 3개와 함께 포함되어 있었다. `.gitignore`는 git만 막지 압축은 못 막는다. 로테이션 필요를 사용자에게 전달.
- 재사용 가치 확인: 265개 역 ↔ TAGO 노드 ID 매핑, 열차 종별 2단 폴백(`vehiclekndid` → `traingradename`), 심야 시각 보정(`getAdjustedHour`), Stem-and-Leaf 렌더링. `scripts/fetch_station_data.py`가 이미 코레일 `travelerTrainRunPlan2`/`travelerTrainRunInfo2`를 호출하고 운전정차(`stop_se_nm`)까지 다루고 있었으나, 환경변수 이름 불일치로 실행 불가 상태.
- 다음에 할 일: 프로젝트 스코프를 한국 전체 철도 시각표로 확정하고, 운영사 공식 배포 엑셀을 백데이터로 쓰는 파이프라인 착수.

## 2026-08-25 — Claude (KTX 시각표 파이프라인 구축)

- 한국철도공사 공식 KTX 시각표(9개 노선) 파서 `parse_ktx.py` 작성. 9개 시트가 하행/상행 2블록 + 3행 헤더(한글·한자·영문)로 완전히 일관되어 파서 하나로 전부 처리. 88개 역 / 621편 / 5,126 정차 추출, 경고 0건.
- 발견한 문제: 미정차가 빈칸이 아니라 `00:00:00`인데, 같은 파일에 `00:03`·`01:04` 같은 **실제 심야 도착 시각**이 공존한다. "0시대=미정차" 규칙을 쓰면 막차가 통째로 사라진다. 정확히 `00:00:00`만 거르는 것으로 처리했으나 개정판에서 깨질 수 있는 지점.
- 시각은 자정 기준 경과 초로 저장하고 단조 증가를 강제해 자정 넘김(42편, 최대 25:17)을 별도 변환 없이 처리. 초 눈금이 15초 단위라 초까지 보존.
- 검증: 역간 소요시간 5~72.5분으로 전부 상식 범위, 시각 역행·열차번호 중복 0건. rail.blue 노트의 열차 1번 예시와 대조해 부산 도착이 07:52 vs 07:50로 다른 것을 확인 — 오류가 아니라 다이어 개정 결과로 판단.
- 다음에 할 일: 광역전철을 같은 스키마에 합류시켜 통합 조회가 되는지 확인.

## 2026-08-25 — Claude (1호선 반영, 역 모델 재설계)

- 수도권 전철 1호선 시각표(평일) 파서 `parse_line1.py` 작성. KTX와 정반대인 전치 구조(역=행, 열차=열)에 역 하나가 2행(도착/출발)을 쓴다. 107개 역 / 840편 / 31,522 정차.
- 성과: 셀 패턴으로 **급행 통과역의 통과 시각까지 확보**(2,957건). 도착·출발이 분리되어 있어 KTX 파일의 "역당 시각 1개" 한계를 보완. `연계열번`(종착 후 이어받는 열차번호)도 `next_train_no`로 살렸다.
- 발견한 문제 (1): 역명이 3글자로 절삭되어 있다(`온양온`→온양온천, `1지청`→청량리 등). 사용자 확인으로 접두사 `1`이 서울 지하철 1호선 지하구간 표기임을 확정하고 매핑 완료, 미확인 0건.
- 발견한 문제 (2): `시흥연`은 역이 아니라 **시흥연결선(분기점)**이었다. 상·하행 모두 도착시각이 한 번도 없는 것으로 판별. `junctions` 테이블로 분리.
- 발견한 문제 (3): 첫 병합에서 서울역을 KTX와 1호선이 한 목록에 섞이게 만들어, "광운대행 다음에 부산행"이 나오는 문제가 발생. 사용자 지적으로 **역 모델을 노선 단위 승강장(`stops`) + 역사 그룹(`station_groups`) 2계층으로 재설계**. 계통(경인·경부장항 등)은 나누지 않아 구로역이 과분할되지 않게 했다. 이 변경으로 이전의 `서울(지하)` 하드코딩 예외가 규칙에 흡수되어 사라졌다.
- 다음에 할 일: 휴일 다이어 확보 — 현재 평일만 있어 주말에 틀린 정보를 제공한다.

## 2026-08-25 — Claude (휴일 다이어 반영, 문서 구축)

- 1호선 휴일 시각표 반영. 시트명에서 운행일 구분을 읽는 구조라 **파서를 한 줄도 고치지 않고 그대로 동작**(729편 / 28,715 정차, 경고 0건). 역 집합도 평일과 정확히 일치(차집합 0). `build_db.py`를 다중 디렉터리 병합 구조로 확장.
- 검증: 구로역 08:00~08:12 기준 수요일 11편 → 일요일 7편으로 갈리는 것을 확인. 계통별 감편 폭은 경인급행이 212→155편(-27%)으로 가장 크고 광명셔틀은 40편 동일.
- 발견한 문제: `휴일` 다이어가 토·일에만 자동 적용된다. 공휴일에도 휴일 다이어로 운행하므로 `calendar_dates` 예외 테이블이 필요하다. 명절 임시열차는 이 파일에 아예 없어 별개 문제로 남는다.
- 현재 적재량: 195 stops / 188 groups / 2,190 trips / 65,363 stop_times / 10.6MB. 1호선 하나에 2개 다이어로 이 크기라 수도권 전 노선 시 100MB 초과 가능성을 제약에 기록.
- README.md 작성 — 팀 표준 골격에 §6(원본 파일의 함정)과 §8(검증 항목)을 추가. 개정판에서 파서가 깨졌을 때 재조사 비용을 줄이기 위한 절이다.
- 다음에 할 일: todo.md·future_plan.md 정리 후, 공휴일 예외 테이블과 광역전철 노선 추가.

## 2026-08-25 — Claude (프로젝트 표준 정리, 코레일 시각표 일괄 확보)

- `RAW/kr_rail.zip`(기존 산출물 백업)을 압축 해제해 표준 폴더 위치로 배치 — README/history/todo/future_plan은 루트로, 파서 3종은 `scripts/`, `line1_stops.csv`는 `data/`, DB는 `output/`. 원본 zip은 사용자 확인 후 삭제.
- `CLAUDE.md`를 팀 표준 양식에 맞춰 실제 내용으로 채움. `japanese_style_timetable`(형제 프로젝트) 참고 지시를 §6에 추가 — TAGO 역 매핑 등 재사용 자산이 있으나 rail.blue 내부 API 이식은 금지.
- 코레일 다운로드 게시판에서 시각표 78건 중 시간표 카테고리 최신본 18개를 `RAW/korail_시각표_20260825/`에 확보. 이 중 심야임시전동열차는 "2026.5.26.限"으로 이미 종료된 한시 운행이라 파싱 대상에서 제외(원본만 보관).
- openpyxl로 17개 파일 구조를 직접 검사해 3그룹으로 분류: 전치 구조(1호선과 동일 코어, 12개), KTX 스타일(열차=행, 2개: KTX/일반열차), 비정형(관광열차). 전체 계획을 `~/.claude/plans/ethereal-herding-crown.md`에 작성 — "동해선"이 전동열차/KTX/일반열차 3개 파일에 서로 다른 실체로 존재하는 등 노선명 충돌 위험을 미리 기록.

## 2026-08-25 — Claude (수인분당선 반영)

- `scripts/parse_suin_bundang.py` 작성. 1호선과 같은 전치 구조지만 헤더가 한 행 더 있어(개정안 제목행) "열차번호" 라벨을 찾아 헤더 위치를 동적으로 잡도록 구현 — 향후 노선마다 행 번호가 다를 수 있음을 반영.
- 역명 63개 전부 나무위키 "수도권 전철 수인·분당선"·"인천역" 문서와 대조해 확정(미확인 `?` 0건). 핵심 발견: `신인천`/`신수원`은 절삭이 아니라 **코레일 전산상 실제 표기**였다 — 인천역·수원역이 경인선(1호선)과 선로가 분리돼 있어 시스템에서 수인분당선측을 별도 이름으로 관리한다(나무위키 "인천역" §2.2에서 명시적으로 확인). `인천`/`수원`으로 정규화해 되돌림.
- `build_db.py`의 하드코딩된 `/home/claude/...` 절대경로(예전 샌드박스 흔적)를 프로젝트 상대경로(`tmp/out_*`, `output/...`)로 수정 — 이전까지는 이 스크립트가 이 프로젝트에서 애초에 실행 불가능한 상태였음. 수인분당선 로딩 블록 추가.
- 검증: build_db.py 실행 결과 `수원`(국철·1호선·수인분당선 3-way), `인천`(1호선·수인분당선), `청량리`(3-way), `수서`(국철·수인분당선)가 station_groups에서 정확히 병합됨 — `신인천`/`신수원` 정규화가 제대로 작동했다는 방증. 역간 소요시간 1~10분으로 상식 범위, 경고 0건.
- 겸사겸사 새로 받은 KTX·1호선 원본으로 기존 파서 재실행 — 둘 다 무수정으로 동작(KTX 621편/경고0, 1호선 평일840·휴일729편/경고 이전과 동일). 최종 DB: 258 stops / 247 groups / 2,969 trips / 91,060 stop_times / 13.6MB.
- 다음에 할 일: 안산과천선(2단계) — 시트 이름 규칙이 수인분당선과 달라(`평일(상)`류) 파싱 로직 재작성 필요.

## 2026-08-25 — Claude (안산과천선 반영)

- `scripts/parse_ansan_gwacheon.py` 작성. 수인분당선과 헤더·전치 구조는 동일, 시트 이름만 `평일(상)`류로 다름. 진접~오이도 51개 역이 한 파일에 다 들어있음(코레일이 서울교통공사 구간 포함 직결 전체 시간표를 배포).
- 나무위키 "수도권 전철 4호선" 문서로 역명 51개 전수 대조. 두 가지 개칭-지연 사례 발견: `당고개`(2024-10 `불암산`으로 개칭됐으나 2026-08-22 갱신본에도 구 역명 잔존), `4동운`(2009년 개칭 전 `동대문운동장`의 흔적, 현재는 동대문역사문화공원). 수인분당선의 `신인천`/`신수원`(전산상 정식 표기)과는 다른 종류의 함정 — 노선마다 역명 특이사항의 성격이 다르므로 매번 나무위키로 확인해야 함을 재확인.
- `시흥기`가 정차역이 아니라 시흥기지(차량사업소)임을 나무위키의 정거장수(51개)와 원본 라벨 수(52개) 대조로 확인, JUNCTIONS로 분리(1호선의 시흥연결선과 같은 패턴).
- `4`로 시작하는 라벨이 절삭이 아니라 호선 구분 접두사(다른 노선의 동명역과 구분용, 예: `4서울`=서울역)라는 걸 확인. 처음에 `4서울`을 `서울역`으로 복원했다가 `build_db.py` 검증 단계에서 KTX/1호선의 `서울`과 안 묶이는 걸 발견 — 팀 컨벤션(역 접미사 없는 표기)에 맞춰 `서울`로 정정.
- 검증: `build_db.py` 결과 서울·금정·동대문·창동이 1호선과, 오이도~한대앞 구간(오이도·정왕·안산·초지·고잔·중앙·한대앞) 7개 역이 수인분당선과 정확히 병합됨을 확인 — 나무위키가 명시한 "4호선-1호선 4번 환승", "수인분당선과 한대앞~오이도 선로공유" 서술과 정확히 일치. 역간 소요시간 1~9분, 경고 0건.
- 최종 DB: 309 stops / 287 groups / 3,842 trips / 123,737 stop_times / 18.0MB.
- 다음에 할 일: 경의중앙선 등 계획 문서 3단계 나머지 노선. 지금까지 2개 노선 모두 핵심 파싱 로직이 동일했으므로, 다음 노선에서 중복이 확실해지면 공통 모듈 추출 검토.

## 2026-08-25 — Claude (경의중앙선 반영)

- `scripts/parse_gyeongui_jungang.py` 작성. 헤더는 1호선과 동일(3행, "개정안" 제목행 없음) — 파서가 "열차번호" 라벨을 찾아 헤더 위치를 동적으로 잡는 방식이라 그대로 대응됨. `연계열번` 행도 1호선과 동일 패턴으로 존재해 next_train_no 처리 재사용.
- 나무위키 "경의·중앙선/역 목록" + 위키백과 "양정역(남양주)"로 역명 53개 전수 대조. `1양정`/`1양원`이 절삭이 아니라 **한국철도물류정보시스템의 내부 등록명**임을 위키백과에서 명시적으로 확인("한국철도물류정보시스템에는 1양정으로 등록되어 있다") — 수인분당선의 신인천/신수원과 같은 부류의 함정이지만 이번엔 다른 역, 다른 이유(전산 등록명 vs 노선 분리 표기)로 발생. `도심`은 반대로 절삭처럼 보이지만 실제 역명(도심역)이었다 — 낯선 2글자 역명을 무조건 절삭으로 의심하면 안 된다는 반례.
- 이 원본은 문산~지평 구간만 포함(임진강~문산 셔틀 구간은 별도 운행이라 미포함)임을 나무위키에서 확인 후 README에 명기.
- 검증: `build_db.py` 결과 청량리가 4-way(국철·1호선·수인분당선·경의중앙선)로, 왕십리·이촌이 각각 수인분당선·안산과천선과, 상봉·덕소·양평·행신이 KTX/일반열차(국철)와 정확히 병합됨을 확인. 역간 소요시간 1~11분, 경고 0건.
- 최종 DB: 362 stops / 331 groups / 4,174 trips / 138,406 stop_times / 20.8MB.
- 다음에 할 일: 경의선/경춘선/경인급행/ITX-청춘/경강선/동해선(전동)/대경선/일산선 중 택1로 계속.

## 2026-08-25 — Claude (경의선 반영)

- `scripts/parse_gyeongui.py` 작성. 경의중앙선과 헤더·`연계열번` 구조가 완전히 같아 파서를 거의 그대로 재사용(역명 alias만 항공대/디엠시 2건).
- 핵심 발견: 경의선과 경의중앙선은 같은 노선이 아니라 **가좌역에서 갈라지는 별개 운행계통**이다 — 경의중앙선은 용산선(용산·청량리 방면)으로, 경의선은 신촌·서울역 방면으로 빠진다. 두 파일이 가좌~문산 19개 역만 선로를 공유. `build_db.py` 실행 후 이 19개 역이 station_groups에서 "역명(경의중앙선),역명(경의선)"으로 정확히 묶이는 것으로 검증.
- 이 파일은 도라산~서울까지 포함해 경의중앙선판에 없던 도라산·임진강·운천이 추가됨. 파싱 결과 `도라산`은 전 트립 통틀어 정차 기록 0건 — 나무위키의 "도라산~임진강 운행 중단" 서술과 정확히 일치(임진강~문산 구간의 희박한 셔틀편은 정차 기록 있음). 미사용 역 자동 검출 로직을 파서에 추가해 확인.
- 검증: 역간 소요시간 1.5~6.5분, 경고 0건, 역명 미확인 0건.
- 최종 DB: 387 stops / 335 groups / 4,280 trips / 140,222 stop_times / 21.1MB(경의선 추가분은 대부분 역 공유라 +0.3MB로 작았음).
- 다음에 할 일: 경춘선/경인급행/ITX-청춘/경강선/동해선(전동)/대경선/일산선 중 택1로 계속.

## 2026-08-25 — Claude (경춘선 반영)

- `scripts/parse_gyeongchun.py` 작성. 헤더·`연계열번` 구조는 경의중앙선과 동일하지만, `광운대시종착(평일)` 시트가 처음 보는 구조였다 — 한 시트 안에 상행(A열, 춘천→광운대)과 하행(E열, 광운대→춘천) 두 블록이 완전히 독립된 역 목록·헤더로 나란히 붙어있음. 다른 노선처럼 "시트 하나 = 역 목록 하나"로 가정하면 두 블록이 뒤섞여 잘못 파싱되기 때문에, 블록별로 `(역명 열, 데이터 열 범위)`를 지정하는 방식으로 파서를 일반화했다.
- 나무위키로 역명 25개 전수 대조. `평내호`→`평내호평` 1건만 절삭(승하차 순위표 1위 역이라 바로 확인됨), 나머지는 전부 실명. 광운대는 원래 1호선 소속 역이지만 망우선을 통해 경춘선과 직결되는 실제 종점임을 확인.
- 검증: 나무위키에 "춘천발 광운대행 열차는 평일에만 하루 2회 운행"이라고 명시돼 있는데, 파싱 결과도 정확히 상행 2편·하행 2편으로 일치 — 2블록 시트 처리 로직이 제대로 작동했다는 강한 증거. `build_db.py` 결과 광운대(1호선·경춘선), 망우·중랑(경의중앙선·경춘선), 회기·상봉·청량리(경의중앙선·경춘선 추가 병합)가 나무위키 환승역 서술과 정확히 일치.
- 최종 DB: 412 stops / 354 groups / 4,496 trips / 144,543 stop_times / 21.9MB.
- 다음에 할 일: 경인급행/ITX-청춘/경강선/동해선(전동)/대경선/일산선 중 택1로 계속.

## 2026-08-25 — Claude (경인급행 갱신)

- 경인급행 파일을 조사하다가 새 노선이 아니라는 걸 발견했다 — 역 26개가 전부 이미 반영된 1호선 데이터(경인 계통)와 겹치고, 원본 1호선 파일에도 `경인급행_평일_상/하` 시트가 이미 있었다(2026.2.28.부터). 이번 파일은 그보다 최신 개정판(2026.5.26.부터)이라, 사용자에게 "건너뛴다 vs 경인급행만 갱신"을 물어 갱신 쪽으로 진행.
- `scripts/parse_gyeongin_express.py` 작성. `parse_line1.py`의 `SKIP_SHEETS`에 옛 경인급행 시트 4개를 추가해 원본 1호선 파일에서 뺐다. `build_db.py`는 이 새 소스를 별도 노선이 아니라 `LINE_OF["LINE1"]`("1호선")로 등록해 기존 1호선 역(구로·영등포 등)과 stop_id를 공유시킴 — 역명 26개 전부 기존 1호선 역명과 정확히 일치(절삭 없음) 확인.
- 검증: 옛 경인급행 편수(평일212+휴일155=367)와 새 파일의 총 편수(367)가 정확히 일치. `build_db.py` 재실행 후 stops/station_groups/전체 trips 총량이 교체 전후로 그대로인 것도 같은 이유(신·구가 같은 서비스의 시간표만 갱신된 것이라 순증가가 없음) — 순수 교체가 성공했다는 증거.
- 이 경험을 README §6에 일반화된 주의사항으로 남김: 새로 받은 파일을 열었을 때 역 목록이 기존 노선과 완전히 겹치면 "새 노선"이 아니라 "기존 서비스의 갱신판"일 수 있다.
- 최종 DB: 412 stops / 354 groups / 4,496 trips(구성만 변경, 총량 동일) / 144,543 stop_times / 22.1MB.
- 다음에 할 일: ITX-청춘/경강선/동해선(전동)/대경선/일산선 중 택1로 계속.

## 2026-08-25 — Claude (경강선 반영, 남은 순서 조정)

- 사용자 지시로 순서 조정: ITX-청춘은 경의중앙선·경춘선과 선로는 같이 쓰지만 전동열차가 아니라 여객열차라, 남은 전동열차 노선(경강선·동해선(전동)·대경선·일산선)을 먼저 끝내고 ITX-청춘을 마지막에 다루기로 함.
- `scripts/parse_gyeonggang.py` 작성. 헤더 구조는 동일. 나무위키로 역명 12개 전수 대조. `신이매`/`신판교`가 수인분당선의 `신인천`/`신수원`과 정확히 같은 부류의 함정(코레일 내부 동명역 구분 접두사)임을 재확인 — 이제 세 번째 노선에서 같은 패턴이 나옴. `세종릉`→`세종대왕릉`, `도예촌`→`신둔도예촌`, `경광주`→`경기광주`는 중간 음절을 건너뛴 압축 표기.
- 검증: station_groups에서 이매(수인분당선), 부발(국철 — KTX 파일의 중부내륙선 시트와 겹침, 나무위키에 "판교~부발 구간 선로 공유"로 명시)이 정확히 병합됨. 역간 소요시간 2~6.5분, 경고 0건.
- 최종 DB: 424 stops / 364 groups / 4,717 trips / 147,108 stop_times / 22.5MB.
- 다음에 할 일: 동해선(전동)/대경선/일산선 중 택1로 계속. 다 끝나면 ITX-청춘.

## 2026-08-25 — Claude (동해선(전동) 반영)

- `scripts/parse_donghae.py` 작성. 헤더·`연계열번` 구조는 다른 노선과 동일. 부전~태화강(부산·울산) 구간. 시종착이 `망양`인 열차는 정차역이 아니라 차량기지 입출고용 중간 시종착(나무위키에 명시) — 경춘선 광운대·안산과천선 시흥기지와 같은 패턴.
- 나무위키로 역명 23개 전수 대조. 대부분 단순 3글자 절삭(`오시리`→오시리아 등), `부교대`→`교대`만 예외로 부산 도시철도 3호선 동명역과 구분하는 접두사로 추정.
- 계획 문서(`~/.claude/plans/ethereal-herding-crown.md`)에서 미리 경고했던 **"동해선 노선명 충돌"이 실제로 발생**했다 — 이 파일(전동)·KTX 파일(동해선(서울~포항)/동해선(부전-강릉) 2계통)·일반열차 파일에 각각 다른 실체의 "동해선"이 있다. `build_db.py`에서 `line_name`을 `동해선(전동)`으로 구분해 등록해서 해결.
- 검증: `build_db.py` 결과 부전·태화강·남창·기장·신해운대·센텀 6개 역이 KTX(국철)의 동해선 시트들과 station_groups에서 정확히 병합됨 — KTX-이음이 같은 구간 선로를 공유하기 때문. 역간 소요시간 1.5~10.5분, 경고 0건.
- 최종 DB: 447 stops / 381 groups / 4,915 trips / 151,450 stop_times / 23.1MB.
- 다음에 할 일: 대경선/일산선 중 택1로 계속. 일반열차 파일의 `동해선` 시트를 나중에 반영할 때도 `동해선(일반)`처럼 구분자 필수. 다 끝나면 ITX-청춘.

## 2026-08-25 — Claude (대경선 반영)

- `scripts/parse_daegyeong.py` 작성. 헤더·`연계열번` 구조는 다른 노선과 동일. 이번엔 역명 절삭이 아니라 **역 구성 자체가 함정**이었다 — 원본 라벨이 14개인데 나무위키의 공식 정차역 수는 8개. 나머지 6개(가천·고모·지천·신동·연화·약목)는 실재하는 역이지만 대경선 열차가 통과만 함(나무위키에 "시간표 파일에는 저 6개 역의 통과 시간도 표시되어 있다"고 명시). 시흥기지(비역)나 도라산(운행 중단)과 달리 JUNCTIONS로 뺄 필요 없이 그냥 일반 역으로 등록 — 기존 stop_type 분류 로직(도착·출발 중 하나만 있으면 `pass`)이 자동으로 처리해줬다.
- 나무위키 대조 결과 역명 14개 전부 절삭 없이 실명임을 확인 — 지금까지 유일하게 STATION_ALIAS가 완전히 빈 노선.
- 검증: `build_db.py` 결과 서대구·동대구·경산이 KTX(국철)와 station_groups에서 정상 병합됨(이 역들에 KTX도 정차). 역간 소요시간 1~12분, 경고 0건.
- 최종 DB: 461 stops / 392 groups / 5,109 trips / 154,038 stop_times / 23.4MB.
- 다음에 할 일: 일산선(계획 문서 3단계 마지막). 끝나면 ITX-청춘.

## 2026-08-25 — Claude (일산선 반영 — 전동열차 노선 전부 완료)

- `scripts/parse_ilsan.py` 작성. 안산과천선과 같은 패턴 — 서울교통공사 구간(지축~오금) 포함 오금~대화 44개 역이 한 파일에 다 들어있음(코레일이 직결 전체 시간표 배포).
- 나무위키 "서울 지하철 3호선/역 목록"으로 역명 44개 전수 대조. `3`으로 시작/끝나는 라벨 8건(3수서·3도곡·3옥수·3충무·3을지·3종로·3대곡·금호3)이 안산과천선의 `4` 접두사와 같은 부류(3호선 구분 표기)임을 확인 — `3`이 접두사로도 접미사(금호3)로도 붙어서 위치로 일반화하면 안 된다는 걸 재확인. `동대입`은 절삭인 줄 알았는데 정식 역명 자체가 `동대입구`였음(`동대문입구` 아님) — 나무위키 대조 없이 진행했으면 틀렸을 뻔.
- 검증: `build_db.py` 결과 수서(국철·수인분당선·일산선 3-way)·종로3가(1호선)·도곡(수인분당선)·충무로(안산과천선)·옥수(경의중앙선)·대곡(경의중앙선·경의선·일산선 3-way)·교대(동해선(전동))가 전부 예상대로 병합됨. 특히 `교대` 병합으로 동해선 반영 때 궁금했던 `부교대` 접두사의 이유(3호선 교대와 동명이라서)가 최종 확인됐다.
- 최종 DB: 505 stops / 429 groups / 5,878 trips / 182,977 stop_times / 28.8MB.
- **계획 문서(`~/.claude/plans/ethereal-herding-crown.md`)의 3단계(전동열차 노선 전부)가 여기서 끝난다.** 전동열차 10개 노선(1호선·수인분당선·안산과천선·경의중앙선·경의선·경춘선·경강선·동해선(전동)·대경선·일산선, 경인급행은 1호선 갱신이라 별도 카운트 제외) 전부 반영. 다음에 할 일: ITX-청춘(여객열차, 계획 마지막 항목). 그 다음은 일반열차(그룹 B, 16개 노선)·관광열차(그룹 C, 비정형).

## 2026-08-25 — Claude (ITX-청춘 반영 — 계획 문서 항목 전부 완료)

- `scripts/parse_itx_chuncheon.py` 작성. 헤더·`연계열번` 구조는 다른 노선과 동일. 여객열차(전동차 아님)라 계획대로 가장 마지막 순서로 진행.
- 경의중앙선(용산~청량리, 경원선 구간)과 경춘선(청량리~춘천)을 그대로 이어 달리는 열차라, **역명 31개가 전부 이미 그 두 노선에서 검증된 것들**이었다 — 새로 나무위키를 확인할 필요가 없었던 유일한 노선.
- 검증: `build_db.py` 결과 31개 역 전부 기존 경춘선·경의중앙선 station_groups로 병합되고 새 그룹은 0개 — 지금까지 반영한 노선들 간 역명 표기가 완벽히 일관됐다는 뜻. 역간 소요시간 0.5~9분, 경고 0건.
- 최종 DB: 536 stops / 429 groups / 5,966 trips / 185,705 stop_times / 29.3MB.
- **이것으로 계획 문서(`~/.claude/plans/ethereal-herding-crown.md`)의 전동열차+ITX-청춘 전 항목이 끝났다.** 남은 건 일반열차(그룹 B, 16개 노선 — `parse_ktx.py`를 다중 노선/시트 대응으로 일반화하거나 별도 스크립트 필요, 동해선/서해선 노선명 충돌 재확인 필요)와 관광열차(그룹 C, 비정형 블록 레이아웃 — 전용 소규모 파서 필요)뿐.

## 2026-08-25 — Claude (일반열차 반영)

- 일반열차 파일을 열어보니 레이아웃이 KTX와도, 지금까지의 전동열차 계열과도 달랐다 — 역=행/열차=열 전치 구조(전동열차 계열과 동일)인데 역당 시각이 1개뿐(KTX와 동일)인 하이브리드 구조. `parse_ktx.py`를 그대로 재사용할 수 없어 `scripts/parse_ilban.py`를 새로 작성.
- 처음 실행에서 경고 67건 발생 — 원인은 실제 데이터 열 뒤에 열 라벨의 한자·영문 버전이 별도 열로 붙어있던 것(`열차번호`→`列車番號`→`Train NO.`). 이 열들을 열차 열로 오인해서 생긴 가짜 트립이었다. 열차번호가 항상 숫자라는 성질로 필터링해서 0건으로 해결.
- 검증 중 최대 소요시간이 1433분(24시간 가까이)으로 나오는 이상치를 발견. 추적해보니 원본 자체의 데이터 오류였다 — 호남선 1491/1492는 이미 종착한 역 다음 행에 이전 열차의 시각 찌꺼기가 남아있었고, 장항선 1246은 "종착역=홍성" 라벨 자체가 틀려 있었다(실제로는 홍성을 지나 평택까지 정상 운행 중인데 라벨만 잘못 박혀 있었음).
- 종착역 라벨을 기준으로 그 이후 데이터를 잘라내는 방식을 시도했다가, 장항선 1246 사례에서 라벨 자체가 틀렸다는 걸 발견하고 오히려 정상 데이터를 잘라먹는 결과가 나와 되돌렸다. 대신 자정 넘김 보정 임계값을 6시간으로 올려 "명백한 24시간대 오류"만 막고, 그보다 작은 역행은 원본 그대로 두고 경고만 남기는 절충안으로 정리 — 완벽한 정합성 검증보다 실용적 경계선을 택함.
- 노선명 충돌(경부선·호남선·전라선·경전선·중앙선이 KTX 시트와 겹침) 방지로 15개 노선명 전부에 `(일반)` 접미사. stops는 KTX와 같은 `LINE_OF["KTX"]`("국철")로 등록해 역을 공유.
- 검증: 안양·오산·평택·천안·아산(1호선), 구미·약목·왜관·신동(대경선)이 station_groups에서 정확히 병합됨. 특히 약목·신동은 대경선 나무위키 문서의 "무궁화호가 일부 정차"라는 서술을 이번에 실제 데이터로 확인한 셈.
- 최종 트립: 393개(호남선 1491/1492·장항선 1246 등 3건 경고 잔존, 사람 재확인 필요 — todo.md 등록).
- 다음에 할 일: 관광열차(그룹 C, 마지막 남은 항목).

## 2026-08-25 — Claude (관광열차 반영 — 코레일 게시판 확보분 전부 완료)

- 관광열차 파일은 지금까지 본 어떤 노선과도 다른 완전 비정형 레이아웃이었다 — 한 시트 안에 서비스 6개(동해산타·백두대간협곡(V-트레인)·남도해양 2계통·서해금빛·정선아리랑), 편성 14개가 손으로 짜 맞춘 듯한 작은 표로 흩어져 있고 행/열 규칙이 없음.
- `scripts/parse_gwangwang.py` 작성. 좌표 하드코딩 대신 "역명" 셀을 전부 찾는 마커 스캔 방식으로 해결 — 역명 헤더 바로 위 행이 편성 제목(`#2502(강릉→영주)`), 그보다 위에서 가장 가까운(같은 열 이하) "OO열차(요일)" 텍스트가 서비스명이라는 규칙만으로 14개 편성을 전부 정확히 찾아냄.
- 첫 실행에서 역명 목록에 `#2512(철암→분천)`, `역명` 같은 글자가 섞여 나오는 버그 발견 — 백두대간협곡열차의 V-트레인 연계편(#2511→#2512→#2514)이 같은 열에서 빈 행 없이 바로 이어 붙어 있어서, "빈 칸을 만나면 역 목록 끝"이라는 종료 조건만으로는 다음 블록의 제목·헤더까지 역명으로 읽어버렸다. 다음 블록의 `#`제목이나 `역명` 헤더를 만나면 멈추도록 종료조건을 추가해 해결.
- 운행요일이 "목~일"(단순 구간), "목~월"(주 경계를 넘는 구간), "2·7일 장날, 토~일"(날짜 기반 임시 증편 포함)까지 섞여 있어, 요일 구간(`~`)만 calendar에 반영하고 날짜 기반 부분은 반영하지 않기로 함(ponytail: 필요해지면 calendar_dates류 예외 테이블로 확장).
- 검증: KTX·일반열차와 같은 국철 선로라 stops를 "국철"로 등록했더니 역 58개 전부 기존 국철 역과 병합되고 새 역은 0개.
- **이것으로 코레일 다운로드 게시판에서 확보한 시각표 17개(그룹 A/B/C) 전부 반영 완료.** 계획 문서(`~/.claude/plans/ethereal-herding-crown.md`)의 모든 항목이 끝났다. 최종 DB: 711 stops / 584 groups / 6,373 trips / 190,920 stop_times / 30.2MB.
- 다음에 할 일: 남은 건 데이터 품질 항목뿐 — 공휴일 calendar_dates 예외, §8 검증 자동화, 일반열차 데이터 이상치 원본 재확인(todo.md 참조).

## 2026-08-25 — Claude (전국 운영주체별 파싱 현황 조사)

- "지금 대응 가능한 노선이 전부 한국철도공사 운영 노선인가?"라는 질문에서 출발해, 나무위키 권역별 전철 문서(수도권·동남권·대구권·대전권·광주권)를 전부 대조해 코레일 바깥에 있는 노선 중 추가로 확보할 수 있는 자료가 있는지 조사했다.
- 결론: 현재 DB는 정의상 "코레일이 운영하거나 대신 배포하는 노선"만 커버하므로, 서울교통공사·부산교통공사·대구교통공사·대전교통공사·광주교통공사·인천교통공사 및 각종 민자 노선은 애초에 코레일 다운로드 게시판 범위 밖 — 이건 파이프라인의 결함이 아니라 데이터 소스의 태생적 경계.
- 조사 중 진짜 갭 하나 재확인: `서해선_전동열차` xlsx는 세션 초반에 이미 다운로드해뒀지만 파싱을 빠뜨렸다(코레일 운영 노선인데도).
- 조사 도중 사용자가 대구 도시철도 1/2/3호선, 대전 도시철도 1호선 원본 4개를 RAW에 직접 확보해 확인 요청 — openpyxl로 구조 확인해보니 둘 다 기존 그룹 A/B와 다른 레이아웃(대구는 헤더 2행짜리 변형, 대전은 "역별 배차표" 방식)이라 신규 파서가 필요함을 확인. 광주 도시철도 1호선은 원본 미확보 상태로 남음.
- 상세 표와 근거: `docs/analysis/260825_1129_전국운영주체별_파싱현황조사.md`. todo.md에 4개 항목(서해선/대구/대전/광주) 등록.

## 2026-08-25 — Claude (서해선(전동) 반영 — 파이프라인 갭 해소)

- 위 조사에서 찾은 진짜 갭(다운로드는 했지만 파싱을 빠뜨린 서해선)을 바로 이어서 처리했다. `scripts/parse_seohae.py` 신규 작성 — 헤더·`연계열번` 구조가 경의선과 완전히 같은 그룹 A 계열이라 `parse_gyeongui.py`를 거의 그대로 재사용.
- 원본 시트에 실제 시각 데이터가 하나도 없는 문산 방면(탄현~판문) 12개 역이 꼬리에 붙어있었다 — 경의선 파일을 복사해서 만들다 안 지운 흔적으로 추정. 경의선 파서처럼 "미사용 역"으로 남기는 대신, 시각 데이터가 전혀 없는 역은 애초에 등록하지 않도록 필터를 추가해 깔끔하게 처리.
- 역명 21개를 나무위키 "서해선/역 목록"(대곡~원시 구간표)과 위치 대조로 전부 확정. 9개가 이미 확인된 "신" 접두사 패턴(신인천/신수원과 같은 부류) — `신초지`→초지, `신신현`→신현, `신신천`→신천, `신소사`→소사 등. 이 중 `신초지`·`신소사`는 실제로 수인분당선/안산과천선의 초지, 1호선의 소사와 물리적으로 같은 역이라 station_groups 병합으로 정확히 검증됨.
- 하나(`신김포`→김포공항)만 기존 패턴과 다른 이례적 형태였다 — "신"+2자 실명도 단순 3글자 절삭도 아니고 둘이 겹친 형태. 원종↔능곡 사이에 오는 역이 나무위키 표에서 김포공항 하나뿐이라는 위치 대조로 확정(5·9호선·공항철도를 아직 안 넣어서 병합 검증은 못 함, 추후 재확인 여지 남김).
- 노선명 충돌 방지 — 일반열차 파일에도 서화성~홍성 구간을 도는 별개의 "서해선"이 이미 있어(계획 문서에서 미리 경고했던 케이스), `line_name`을 `서해선(전동)`으로 구분해 등록.
- 최종 DB: 732 stops / 597 groups / 6,693 trips / 196,856 stop_times / 31.3MB. 단조 증가·소요시간(57.5~68.5분) 검증 통과, 경고 0건.

## 2026-08-25 — 세션 마무리

- 오늘 세션에서 서해선(전동)을 마지막으로 코레일 다운로드 게시판 확보분 18개 시각표 전부 반영을 완료했고, 그 다음 비코레일 운영주체(수도권·동남권·대구권·대전권·광주권) 전수 조사로 이어가 대구·대전 파일이 이미 RAW에 있다는 것과 구조상 신규 파서가 필요하다는 것까지 확인했다.
- 오늘 세션 시작 시점 대비 종료 시점: DB 30.2MB(17개 시각표) → 31.3MB(18개 시각표, 서해선 추가). 코레일 운영분은 이걸로 사실상 전부 확보 완료 상태.
- 다음 세션 목표는 사용자 지시로 확정 — 대구 도시철도 1/2/3호선 + 대전 도시철도 1호선(둘 다 비코레일, RAW 확보 완료) 파싱. `todo.md` 노선 추가 항목과 `docs/analysis/260825_1129_전국운영주체별_파싱현황조사.md` 참조.
- 파이썬 캐시 없음(확인 완료), git 미초기화 상태라 커밋 없음.


## 2026-08-26 — Claude (대구 1/2/3호선 + 대전 1호선 반영, 전체 비코레일 로드맵 수립)

- 사용자가 RAW 폴더를 재조사해 예상보다 훨씬 많은 미반영 자료(서울교통공사 2·3·5~9호선, 인천1호선, 공항철도, 신분당선, 부산1~4호선, 부산김해경전철)와 3가지 추가 확보 방법론(운영사 홈페이지 크롤링 8곳, 배차간격 기반 추정 2곳, 사진 시각표 육안전사 1곳)을 제시 — `/plan`으로 전체 8단계 로드맵을 세워 승인받음(`~/.claude/plans/bubbly-meandering-pudding.md`). 순서: 안정적 방식(대구·대전→부산→서울교통공사→인천1·공항철도) 먼저, 신규 방법론(크롤링·추정·사진·신분당선 PDF/PNG) 나중.
- 1단계 착수: 대구 도시철도 1/2/3호선(대구교통공사) + 대전 도시철도 1호선(대전교통공사) 반영. 상세는 todo.md 해당 항목(51~52번) 참조 — 요지: 대구는 헤더 2행 + 시각이 텍스트('HH:MM:SS')로 저장된 신규 레이아웃, 대전은 "출발역 기준 배차표"라 FIFO 그리디 매칭으로 trip을 복원하는 새 알고리즘이 필요했다.
- **가장 중요한 발견**: `build_db.py`의 `add()`가 순수 이름 매칭으로 station_groups를 병합하는데, 서로 무관한 도시의 노선망을 섞기 시작하니 실제 DB를 빌드해봐야 오병합이 드러났다(용산·신기·대곡·죽전·대공원·교대·신천·중앙로·구암 9건 — 전부 흔한 지명이 대구/대전/서울/수도권에 각각 다른 실제 역으로 존재). `CITY_DISAMBIGUATE` 딕셔너리로 분리해 수정. 반대로 대구역/동대구역은 "역" 접미사 때문에 대경선과 병합이 안 되고 있던 것도 정규화해 고쳤다(둘 다 실제 도보환승역, 위키 확인). 앞으로 노선 추가마다 빌드 후 "복수 노선 역사" 목록 전수 점검이 필수 — todo.md에 상시 체크리스트 항목으로 등록.
- 최종 DB: 848 stops / 707 groups / 9,799 trips / 285,301 stop_times / 47.8MB(31.3MB→47.8MB). 신규 4개 노선 전부 단조증가 위반 0건.
- 다음 세션: 부산 1~4호선 + 부산김해경전철(계획 문서 2단계, RAW 확보 완료·구조 미조사).

## 2026-08-26 — Claude (부산 1~4호선 + 부산김해경전철 반영)

- 계획 문서(`~/.claude/plans/bubbly-meandering-pudding.md`) 2단계 진행. `scripts/parse_busan.py`(신규, 부산1~4호선 통합 1파일 공용, 노선당 별도 출력폴더로 분리) — "열차=행/역=열"(KTX와 같은 방향)에 한 시트 안에 상행·하행 두 블록이 나란히 있는 구조, 대구처럼 평일/토요일/휴일 3종 다이어. `scripts/parse_busan_gimhae.py`(신규) — 역명에 전부 "역" 접미사가 붙어있어 벗겨서 등록.
- 대구 때 도입한 "빌드 후 복수노선 역사 전수 점검" 습관이 실제로 효과를 봤다 — 오병합 4건(금곡·남산·중동·연산)을 추가로 잡았다. 전부 "부산에도 있고, 전혀 다른 지역(남양주·대구·부천·충남 논산)에도 같은 이름의 역이 있는" 패턴. 연산은 부산1↔3호선 자체 환승이 진짜라 부산 쪽 대신 반대쪽(`parse_ilban.py`의 국철 연산, 충남 논산)에 `RENAME` 딕셔너리를 새로 추가해 고쳤다 — 두 노선망이 각자 내부적으로 옳은 병합을 갖고 있을 때는 "가짜 쪽"을 고쳐야 진짜 병합이 안 깨진다는 걸 배움.
- 지난 세션에 고친 동해선(전동) '교대'→'교대(부산)' 수정이 이번에 재검증됐다 — 부산1호선의 '교대'가 실제로 그 역과 2018년 개통 환승통로로 연결된 진짜 환승역임을 웹검색으로 확인. 구포·부전·화명·사상·좌천·벡스코·거제·동래·서면·덕천·수영·대저·미남도 전부 실제 환승/동일지역역으로 확인 후 그대로 병합.
- 최종 DB: 984 stops / 827 groups / 14,290 trips / 408,906 stop_times / 69.7MB(47.8MB→69.7MB). 신규 5개 노선 전부 단조증가 위반 0건.
- 다음 세션: 서울교통공사 2·3·5~9호선(계획 문서 3단계) — 평일/휴일판 기준일 격차, 8호선 휴일 후보 2개 확정, 파일 구성 복잡(합본 다수)이라 파일 단위로 먼저 구조 확인 필요.

## 2026-08-26 — Claude (동명이역 병합 메커니즘 재설계: merge_key)

- 부산 반영 직후 사용자가 지적: "타 운영기관인데 역 이름이 같은 케이스가 앞으로 더 많을 텐데, 지금처럼 `교대(부산)`같이 표시명 자체를 바꿔버리면 사용자 화면에 지역 접두사가 그대로 노출되는 것 아니냐 — 내부적으로는 지명을 붙인 가상 역명(논산연산/부산연산)으로 저장하고 UI에서만 깨끗하게 보이면 되지 않겠냐"는 제안.
- 옳은 지적이라 즉시 재설계했다. `scripts/build_db.py`의 `add()`/`gid()`에 `merge_key` 매개변수를 추가 — `station_groups.name_ko`/`stops.name_ko`는 항상 깨끗한 역명 그대로 두고, 병합 여부는 각 파서가 `stops.csv`에 심는 `merge_key` 컬럼(빈 값이면 역명 자체가 키)으로만 결정한다. 이 값은 DB 어디에도 저장되지 않는 빌드 시점 내부 키다.
- 영향받은 5개 파서(`parse_donghae.py`, `parse_daegu.py`, `parse_daejeon.py`, `parse_busan.py`, `parse_ilban.py`) 전부 "표시명 자체를 바꾸는" 방식(`CITY_DISAMBIGUATE`가 문자열을 리네임)에서 "표시명은 그대로 두고 merge_key만 얹는" 방식으로 수정. 재설계 중 버그 하나 발견 — 대구1호선의 '교대'를 부산1호선과 같은 `merge_key`("부산교대")로 잘못 묶어놨었다(대구·부산·서울 세 도시가 각자 다른 실제 '교대' 역을 갖고 있는데, 대구 것도 부산 것과 합쳐질 뻔함). 빌드 후 "복수 노선 역사" 점검으로 바로 잡아 3개 도시 모두 정확히 분리(대구교대/부산교대/서울(무표기) 3개 그룹, 화면엔 셋 다 그냥 '교대').
- 재검증: groups 827개(재설계 전과 동일), 전체 재빌드 후 대구·대전·부산·부산김해·동해선·일반열차 전부 단조증가 위반 0건(일반열차의 기존 알려진 이상치 3건 제외). README §4.2/§8에 merge_key 메커니즘 문서화, `meta.station_merge_key`에 상세 설명 추가.
- 다음 세션: 서울교통공사 2·3·5~9호선(계획 문서 3단계) — 이제 이 merge_key 체계가 있으니 서울 자체 동명역(전국 각지와 겹칠 수 있는 흔한 지명)도 같은 절차로 처리하면 된다.

## 2026-08-26 — Claude (서울교통공사 2·5·6·7·8·9호선 반영)

- 계획 문서 3단계. 6개 노선 모두 신규 파서(`scripts/parse_seoul{2,5,6,7,8,9}.py`) — 노선마다 레이아웃이 조금씩 달라 5가지 변형이 나왔다(순환선 방향 표기, 명시적 라벨 vs 암묵적 2행1조, 헤더 라벨 신구 차이, datetime vs 텍스트 시각 혼재).
- **3호선은 사전 조사로 스킵 결정** — RAW의 "서울3호선" 파일이 실제로는 이미 반영된 일산선(코레일 배포본)과 동일한 실제 운행 데이터임을 열차 도착시각 직접 대조(대화역 05:16 출발 열차가 양쪽에서 정확히 일치)로 확인. 사용자가 "코레일도 동일 자료 배포"라고 언급한 그대로였다.
- 8호선은 RAW 후보가 3개였는데 2025-06-02 최신본이 평일+휴일을 한 파일에 다 담고 있어서 그것 하나만 쓰고 나머지 2개(2023/2024)는 폐기 — "최신판 하나로 평일/휴일 둘 다 확보되면 구버전은 버린다"는 원칙이 처음으로 명확히 적용된 사례.
- 대구/부산 때 만든 merge_key 점검 습관이 이번에도 효과를 봤다 — 오병합 6건(송정·월드컵경기장·신흥·증산·종합운동장·상동)을 추가로 잡았다. 전부 "서울에도 있고 부산/대전/경남에도 같은 이름의 역이 있는" 패턴. 이걸로 지금까지 대구+대전+부산+서울 반영을 거치며 발견한 오병합이 누적 19건.
- 역명 정규화도 다수 발생 — 특히 "총신대입구(이수)"는 안산과천선(4호선)에 이미 등록된 표기와 통일해야 진짜 환승역으로 병합됨을 확인, "자양(뚝섬한강공원)"은 2024년 2월 개명을 반영.
- 최종 DB: 1,238 stops / 1,017 groups / 18,789 trips / 581,875 stop_times / **101.3MB(69.7MB→101.3MB, 100MB 돌파)**. 6개 노선 전부 단조증가 위반 0건.
- **DB 크기가 100MB를 넘어서 todo.md에 압축·분할 배포 판단 항목을 올렸다** — 사용자에게 다음 세션에서 확인 필요.
- 다음 세션: 인천1호선(.xls) + 공항철도 (계획 문서 4단계).

## 2026-08-26 — Claude (서울3호선 재확인 — 놓친 출퇴근 증편 열차 5개 발견)

- 직전 세션에서 "서울3호선 파일은 일산선과 완전히 동일해서 스킵"이라고 결론냈던 걸 사용자가 "출퇴근열차 추가였던 것 같은데 확인해달라"고 재확인 요청. 스팟체크(열차 1~2개 시각 대조)만으로 "동일하다"고 판단한 게 안일했다.
- 전체 열차의 (방향, 기점, 종점, 출발시각) 시그니처로 전수 대조한 결과, 실제로 이 파일에만 있는 열차 5개를 발견 — 전부 출퇴근 시간대(05:15/08:04/09:17/16:51/18:00). 사용자 기억이 맞았다.
- 대조 과정에서 두 원본의 자정 넘김 시각 표기 방식이 다르다는 것도 발견(일산선=0시로 랩어라운드하는 datetime 값, 서울3호선="24:02:00" 같은 24시+ 텍스트) — 절대 초로 비교하면 이미 있는 심야 열차가 표기 차이 때문에 "새 열차"로 오검출된다. mod 86400(하루 중 시각)으로 비교해 해결.
- `scripts/parse_ilsan.py`에 서울3호선 파일을 보충 소스로 읽어 시그니처 대조 후 없는 것만 추가하는 로직을 넣었다(전체 교체 아님 — 서울3호선 파일에 휴일 시트가 없어서 기존 일산선 휴일 데이터를 그대로 신뢰). 최종 774 trip(기존 769+신규 5), 단조증가 위반 0건.
- **교훈**: "다른 소스와 데이터가 같아서 반영 불필요"라고 판단할 때는 전체 시그니처 대조까지 해야 한다 — 일부만 확인하고 "동일하다"고 결론내리면 이번처럼 부분적 차이(증편분)를 놓친다.
- DB: stops 1,238(변화 없음, 기존 44개 역 재사용) / trips 18,794(+5) / stop_times 582,005.

## 2026-08-26 — Claude (인천1호선 + 공항철도 반영 — 계획 문서 4단계 완료)

- `scripts/parse_incheon1.py`(신규) — 원본이 `.xls`(구버전 바이너리)라 openpyxl이 못 열어서 이 프로젝트 최초로 `xlrd` 의존성을 추가했다. 구조는 KTX와 같은 "열차=행/역=열, 역당 시각 1개". 평일/**토휴일**(토요일+휴일을 하나로 묶은 다이어) 2종 — 대구·부산과 다른 패턴이라 "노선마다 확인 필요"였던 토요일 다이어 여부가 한 번 더 다르게 확인됨.
- `scripts/parse_airport_railroad.py`(신규) — 헤더 5행(열차번호/시발역/시발시간/종착역/종착시간) + 한 시트 안에 하행/상행 두 블록이 세로로. `수색직결선`은 원본에 시각 대신 '---'로 통과 표시돼있어 실제 정차역이 아니라 junctions로 분리(안산과천선 시흥기지와 같은 부류). `지상서울`·`수색`은 등록은 했지만 평일·휴일·양방향 전부 실제 정차 기록이 0건 — 경의선 '도라산'과 같은 "자리는 있지만 안 쓰는 역" 패턴.
- 오병합 0건 — 대구/대전/부산/서울처럼 다른 도시 동명역 충돌이 이번엔 없었다(인천·공항철도 특유 지명이 많아서인 듯). 부평·부평구청·계양·원인재·서울·공덕·홍대입구·디지털미디어시티·김포공항·마곡나루가 전부 실제 환승역으로 정상 병합됨을 확인.
- 최종 DB: 1,287 stops / 1,055 groups / 20,162 trips / 611,441 stop_times / 105.5MB(101.3MB→105.5MB). 두 노선 전부 단조증가 위반 0건.
- 계획 문서(`~/.claude/plans/bubbly-meandering-pudding.md`) 4단계까지 종료 — "안정적 방식(기존 openpyxl류 파서)" 그룹이 전부 끝났다. 다음부터는 신규 방법론(웹크롤링 8곳 → 추정 2곳 → 사진 1곳 → 신분당선 PDF/PNG) 그룹으로 넘어간다.

## 2026-08-26 — Claude (웹크롤링 1번째: 김포골드라인 반영, 공유 재구성 모듈 분리)

- 계획 문서 5단계 착수(웹크롤링 8곳 중 1번째). 김포골드라인은 RAW 파일이 없어 홈페이지(gimpogoldline.com)를 직접 크롤링해야 했다.
- **핵심 발견**: 이 사이트의 "역 탭"은 JS 클릭 시 서버에 새로 요청하는 게 아니라 이미 로드된 DOM 안에서 CSS 표시만 바꾸는 방식이었다. 처음엔 브라우저로 탭을 하나씩 클릭하고 `get_page_text`로 긁으려 했는데, 클릭 직후 텍스트가 갱신되기 전 상태를 읽어버리는 stale-click 문제가 반복 재현됐다(같은 좌표인데 페이지 리플로우로 위치가 달라지는 문제까지 겹침). `javascript_tool`로 DOM을 직접 읽어(`td.hour`/`td.minute-w`/`td.minute-h` 클래스로 역마다 이미 다 있는 걸 확인) 전 역 데이터를 한 번에 추출하는 방식으로 전환하니 훨씬 안정적이었다 — 앞으로 남은 크롤링 사이트(GTX-A·광주1호선)도 착수 시 이 방식을 먼저 시도할 것.
- 추출한 JSON은 대화창 tool 출력이 컸던 문제를, JS에서 Blob 다운로드를 트리거해 로컬 파일로 받는 방식으로 해결(`RAW/web_크롤링/김포골드라인/gimpo_goldline_raw.json`).
- `scripts/_station_list_reconstruct.py` 신규 — 지금까지는 "노선마다 파서 분리"가 원칙이었지만, 이 FIFO 그리디 매칭 재구성 알고리즘(대전1호선에서 처음 만듦)만은 예외로 공유 모듈로 뽑았다(Rule of Three — 웹크롤링 노선 전부가 같은 "역별 발차 시각 목록" 형식이라 필요). `parse_daejeon.py`를 이 모듈을 쓰도록 리팩터, 회귀 없음(774 trip 그대로) 확인.
- 시각 표기에 괄호가 섞여있는 걸 발견(예: `(08) 10 13`) — 사이트에 범례가 없고 CSS 스타일로도 구분이 안 돼 정확한 의미를 못 찾았다. 실제 승차 가능한 시각이라는 점은 분명해서 괄호만 벗기고 다른 시각과 동일하게 취급, 파서 docstring에 미해결 사항으로 남겨뒀다.
- 오병합 2건 발견(마산: 경전선 마산(경남 창원) vs 김포마산, 고촌: 부산4호선 고촌 vs 김포고촌) — merge_key로 분리. 김포공항에서 공항철도·5·9호선·서해선(전동)과는 정상 병합.
- 최종 DB: 1,297 stops / 1,064 groups / 21,028 trips / 619,302 stop_times / 106.6MB(105.5MB→106.6MB). 단조증가 위반 0건(866 trip).
- 다음: 신림선(정적 HTML, http만 접속) → 우이신설선 → GTX-A 2편 → 광주1호선.

## 2026-08-26 — Claude (김포골드라인 괄호 의미 확인 — 종착역 분기 정정)

- 직전 세션에서 "괄호 의미를 몰라서 무시하고 동일 취급"으로 남겨뒀던 걸 사용자가 바로 확인해줬다 — **괄호=양촌행 열차, 비괄호=구래행(단축) 열차**. 양촌역이 차량기지 성격이라 운행 횟수가 적다는 배경 설명도 받음.
- 원래 코드는 "구래(양촌) 방면" 표 안의 모든 시각을 하나의 목록으로 합쳐 FIFO 재구성했는데, 이 표 안에 실제로는 종점이 다른 두 종류의 열차(양촌행/구래행)가 섞여 있었던 것 — 괄호로 미리 분리하지 않으면 서로 다른 물리적 열차의 시각을 잘못 이어붙일 위험이 있었다(단조증가 검증은 통과해도 종착역이 틀릴 수 있는, 발견하기 어려운 조용한 오류 유형).
- `parse_gimpo_goldline.py`를 상행(양촌 방면)만 괄호/비괄호 두 그룹으로 완전히 분리해 각각 별도로 FIFO 재구성하도록 수정(하행은 괄호가 전혀 없어 그대로 유지). 재구성 결과 상행 중 구래행이 65%, 양촌행이 35%로 나뉨 — "양촌역 운행 횟수가 적다"는 사용자 설명과 정확히 일치.
- 단조증가 위반 0건 유지(866 trip, 총 trip 수 변화 없음 — 종착역만 정확해짐). DB는 stop_times만 소폭 증가(구래 종착 열차에 근사 종착시각 추가), 106.6MB 유지.
- **교훈**: 정체불명 표기를 "일단 무시하고 진행" 처리한 뒤에도, 사용자가 실제 도메인 지식을 갖고 있으면 언제든 재확인해서 바로잡는 게 맞다 — 이번 경우 검증(단조증가)을 통과했다고 해서 데이터가 맞다는 보장은 아니었다.

## 2026-08-26 — Claude (웹크롤링 8곳 중 2번째 — 신림선 반영)

- sillimlrt.com 직접 크롤링(RAW 없음, 운영사 엑셀 미배포). https 인증서가 깨져있어 http로만 접속 확인, 역별 페이지 11개(`01010201.html`~`01010211.html`, 순차 URL)를 curl로 확보 — 정적 HTML이라 브라우저 자동화 없이 Bash만으로 끝났다(계획 문서에서 예상한 대로).
- 역 페이지마다 `<table>` 6개가 고정 순서(평일 요약/하행/상행, 주말·공휴일 요약/하행/상행)로 있음을 확인. 일부 역(예: 06번 보라매병원)의 주말·공휴일 테이블 `<caption>` 텍스트가 "평일"로 잘못 표기돼 있었다 — 실제 분(分) 값을 비교해 사이트 자체 오타임을 확인, 캡션이 아니라 테이블 위치(인덱스 1/2/4/5)로 구분해 영향 없이 우회.
- 원본이 김포골드라인·대전1호선과 같은 "출발역 기준 배차표" 구조라 `_station_list_reconstruct.link_trips()`를 그대로 재사용(신규 파서 로직 없이 `scripts/parse_sillim.py` 신규 작성만으로 끝남). 괄호 등 목적지 분기 표기는 전 역에서 확인했으나 관찰되지 않음(단일 종점 노선이라 예상대로).
- 역명 충돌 사전 점검: 신림(2호선)·보라매(7호선)·샛강(9호선)은 이름이 그대로 같아 자동 병합되도록 뒀고, 대방(성애병원)은 1호선 '대방'과 표기가 달라 자동 병합이 안 되므로 merge_key="대방"을 명시.
- 빌드 후 "복수 노선 역사" 전수 점검 — 위 4건 전부 실제 환승역으로 정상 병합됨을 확인, 새로운 오병합 0건.
- 단조증가(시각 역행) 검증: 707 trip 중 역행 0건. 단, 같은 분(分)에 두 인접 역이 겹치는 경우 1건 발견 — 역행이 아니라 동시각이라 README §8 기준("시각 역행=오류")상 정상으로 판단(분 단위 반올림 한계).
- 최종 DB: 1,308 stops / 1,071 groups / 21,735 trips / 627,093 stop_times / 102.9MB.
- 다음: 우이신설선(`f_route()` JS 메커니즘 재확인 필요) → GTX-A 2편 → 광주1호선.

## 2026-08-26 — Claude (웹크롤링 8곳 중 3번째 — 우이신설선 반영)

- ui-line.com 직접 크롤링(RAW 없음). 계획 문서에서 "역 링크가 `javascript:f_route('05')`라 폼 재제출 방식으로 추정, 착수 시 정확한 메커니즘 재확인 필요"라고 남겨뒀던 항목을 실제로 확인 — `f_route()` 정의를 찾아보니 `location.href = "/html/intro/intro00/intro_00_" + arg + ".php?pGubn=T2"`인 단순 페이지 이동이었다. 신림선과 마찬가지로 curl 순차 GET만으로 역별 페이지 13개를 확보, 브라우저 자동화 불필요.
- 역 페이지마다 `<table>` 4개(평일 요약/상세, 주말 요약/상세) — 신림선과 달리 상세 테이블 하나에 하선(북한산우이 방향)/시각/상선(신설동 방향) 3열이 같이 들어있어(양방향 분리 안 됨) 파서 파싱 로직을 그에 맞게 새로 짰다. daytype도 평일/주말 2종뿐(토요일 별도 없음, 신림선과 동일).
- 원본이 김포골드라인·신림선과 같은 "출발역 기준 배차표" 구조라 `_station_list_reconstruct.link_trips()`를 그대로 재사용 — `scripts/parse_ui_sinseol.py` 신규 작성만으로 끝남. 괄호 등 목적지 분기 표기는 전 역에서 확인했으나 관찰되지 않음.
- 역명 충돌 사전 점검: 성신여대입구(안산과천선/4호선)·보문(6호선)·신설동(1호선)은 실제 환승역이라 자동 병합되도록 뒀다 — merge_key 없이도 3건 모두 예상대로 정상 병합됨을 빌드 후 확인, 새 오병합 0건.
- 단조증가(시각 역행) 검증: 946 trip 중 역행 0건.
- 최종 DB: 1,321 stops / 1,081 groups / 22,681 trips / 639,312 stop_times / 104.5MB.
- 다음: GTX-A 운정중앙~서울/수서~동탄 → 광주1호선.

## 2026-08-26 — Claude (웹크롤링 8곳 중 4·5번째 — GTX-A 운정중앙~서울/수서~동탄 반영)

- gtx-a.com 직접 크롤링(RAW 없음). 계획 문서에서 "JS 렌더링(SPA), 브라우저 자동화 필요"로 예상했던 대상인데, 실제로는 홈페이지 자체만 curl 등 자동화 도구의 User-Agent를 감지해 차단하고("PAGE BLOCK" 응답), 시각표를 채우는 내부 API(`/trainTimeTable.do?stNo=X101&lineCd=L08&dayTypeCode=1`)는 일반 User-Agent+Referer 헤더만 실으면 curl로도 그대로 JSON을 내려준다는 걸 발견 — 브라우저는 이 API 경로를 찾는 정찰(역 클릭 시 `read_network_requests`로 실제 호출 확인) 단계에서만 쓰고, 실제 데이터 9개 역치는 curl 순수 요청으로 끝났다.
- 역 목록: L08(운정중앙~서울역) 5개 역(운정중앙·킨텍스·대곡·연신내·서울역, 역번호 X101~X106인데 X104는 결번), L09(수서~동탄) 4개 역(수서·성남·구성·동탄, X108~X111). 두 구간이 물리적으로 분리 운행 중이라 line_name을 "GTX-A(운정~서울)"/"GTX-A(수서~동탄)"으로 구분해 등록.
- `dayTypeCode` 파라미터에 1(평일)은 정상 데이터, 2(휴일)는 빈 리스트만 반환됨을 확인 — 프론트엔드 JS 주석에도 "기본값: 평일(나중에 휴일 선택 기능 추가 예정)"이라고 적혀 있어, 버그가 아니라 GTX-A가 아직 휴일 시각표를 공개하지 않은 것으로 판단하고 평일만 반영.
- 원본이 김포골드라인·신림선·우이신설선과 같은 "역별 발차 시각 목록"(UP_TIME_N/DN_TIME_N) 구조라 `link_trips()` 그대로 재사용. `scripts/parse_gtxa.py`(신규). 이 노선은 특이하게 **모든 trip이 종점까지 완주하는 chain으로만 복원됨**(단축 운행 0건) — 급행 전용 신규 노선이라 실제로 전 열차가 완주하는 것으로 보인다.
- 역명 충돌 사전 점검: 대곡·연신내·수서·성남·구성·동탄은 이름이 그대로 같아 자동 병합되도록 뒀고(전부 실제 환승역), 서울역은 기존 그룹명이 '서울'(역 접미사 없음)이라 자동 병합이 안 되므로 merge_key="서울"을 명시. 운정중앙·킨텍스는 단독역사라 신규 등록.
- 빌드 후 "복수 노선 역사" 전수 점검 — 위 7건 전부 실제 환승역으로 정상 병합됨을 확인, 새로운 오병합 0건.
- 단조증가(시각 역행) 검증: L08 282 trip, L09 120 trip 전부 역행 0건.
- 최종 DB: 1,330 stops / 1,083 groups / 23,083 trips / 641,202 stop_times / 104.8MB.
- 다음(마지막 웹크롤링 대상): 광주1호선 소태·녹동 방향(cyber station 모달, 평일/토요일/휴일/명절 4종 다이어).

## 2026-08-26 — Claude (웹크롤링 8곳 중 6·7·8번째, 마지막 — 광주1호선 반영, 계획 문서 5단계 완료)

- grtc.co.kr 사이버스테이션 직접 크롤링(RAW 없음). 역 클릭 → 모달 열기 → "열차시각표" 탭 클릭 → DOM(class="pd"/"nd", data-daycd로 평일=WEEK/토요일=SAT/휴일=DAYOFF/명절=HOLI 확인)에서 시각 추출, 20개 역 전부 브라우저로 순회. 정찰 중 실제 데이터를 채우는 내부 API(`/cyber/portlet/subwayTimetable`, POST)를 네트워크 로그로 발견했지만 세션/폼 파라미터를 재현하지 못해(403) DOM 직접 추출로 대체했다. 큰 JSON을 직접 반환받으면 도구 출력이 특정 길이(대략 1500자)에서 조용히 잘려나가는 현상을 재확인 — 역/다이어별로 잘게 쪼갠 컴팩트 텍스트 포맷("시:분,분;시:분,분")으로 4번씩 나눠 받아 우회했다(Blob 다운로드는 이번 사이트에서 Chrome의 "동일 출처 반복 다운로드 자동 차단"에 걸려 실패).
- **목적지 분기(소태행/녹동행) — 계획 문서가 우려했던 "색상 정보라 get_page_text로 못 뽑는다"는 문제를 실제로는 겪지 않았다.** 사이버스테이션이 이미 CSS class(`nd`)와 DOM 텍스트 라벨("녹동행")로 녹동행 열차를 명시적으로 구분해 두고 있어, DOM에서 class 목록만 확인하면 스크레이핑 시점에 바로 판별 가능했다.
- **재구성 중 발견한 함정**: 소태역 자체의 상행(ND) 기록은 "소태에서 종착하는 열차 도착"이 아니라 **녹동까지 연장 운행하는 열차의 출발 기록만** 남아있었다(소태에서 종착하는 대다수 열차는 거기서 멈추니 "그 다음 역으로의 출발" 자체가 없어 기록이 안 됨). 이걸 모르고 평동..소태 19개 역을 한 번에 FIFO 재구성했더니 대다수 체인이 학동에서 끊기는 이상 현상 발견 → 트렁크(평동~학동, 18개 역)까지 먼저 재구성한 뒤, 학동 도달 체인의 시각을 소태의 (전부 녹동행인) 실제 기록과 별도로 다시 한 번 link_trips로 매칭하는 2단계 구조로 수정. 매칭되면 녹동행(소태 실제 시각 + 녹동까지 추정 연장), 안 되면 학동 시각 기준 추정으로 소태 종착 처리.
- 원본 데이터 이상치 1건(금남로4가역 휴일(명절) 다이어 20시대 "99분" — 앞뒤 값이 13분 간격 패턴이라 59의 오탈자로 판단해 보정).
- 역명 충돌 사전 점검: 화정(일산선 고양시 화정과 무관)·운천(경의선 파주시 운천과 무관) merge_key로 분리, 광주송정역은 KTX·일반열차 '광주송정'과 실제 환승역이라 merge_key로 병합.
- **부수 발견 및 수정(중요)**: 대구·부산·광주1호선처럼 평일/토요일/휴일 3종 다이어를 쓰는 노선의 "휴일"은 일요일 전용인데, 국철·1호선 등 대다수 노선의 2종 다이어(평일/휴일=토+일)도 문자열이 똑같이 "휴일"이라 `build_db.py`의 `INSERT OR IGNORE`가 먼저 들어온 2종 다이어 쪽 정의(토+일)로 조용히 고정시켜버리고 있었다 — 즉 **대구·부산 반영 이후 지금까지 두 노선의 "휴일" 트립이 실제로는 항상 토+일 취급되고 있었던 잠재 버그**를 이번에 처음 발견했다. `SUN_ONLY_LABEL()` 헬퍼를 추가해 3종 다이어 노선의 "휴일"만 "휴일(일요일)"로 이름을 분리해 대구·부산·광주1호선 전부 정정했다(트립 데이터 자체는 그대로, calendar 매핑만 정정 — 재스크레이핑 불필요).
- 단조증가 위반 0건(810 trip, up 방향 목적지 소태 329 vs 녹동 76 — 사용자가 미리 알려준 "양촌역처럼 차량기지 보상성격이라 운행 횟수 적음"과 같은 부류의 분기 구조와 일치).
- 최종 DB: 1,350 stops / 1,102 groups / 23,893 trips / 656,741 stop_times / 106.7MB.
- **웹크롤링 8곳(계획 문서 5단계) 전부 완료.** 다음: 6단계(추정 기반) 인천2호선·용인경전철 → 7단계(사진 기반) 의정부경전철 → 8단계 신분당선.

## 2026-08-26 — Claude (추정치 기반 2곳 — 인천2호선·용인경전철 반영, 계획 문서 6단계 완료)

- ictr.or.kr(인천교통공사)와 ever-line.co.kr(용인에버라인) 둘 다 curl 정적 GET으로 확보(브라우저 불요). 정찰 중 인천교통공사 사이트가 1호선은 분 단위 상세 시각표를 공개하는데 2호선은 배차간격표+역별 첫차/막차만 공개하는 걸 확인 — 처음엔 URL(`timetable.jsp`)이 "1호선" 탭인데 검단호수공원 등 2호선 역이 나와서 혼동했으나, 실제로는 인천1호선의 진짜 종점이 송도달빛축제공원~검단호수공원이라 정상(기존 XLSX 기반 인천1호선 데이터가 이미 이 34개 역을 다 포함하고 있어 추가 반영 불필요함을 확인). 용인경전철도 같은 부류(배차간격+첫차막차만 공개, 요일별 값은 배차간격에만 있고 첫차/막차는 요일 구분 없이 하나씩).
- **근사 생성 방법론**: `scripts/_headway_estimate.py`(신규 공유 모듈) — 방면별 시발역의 첫차~막차를 시간대별 배차간격으로 채워 기준 발차 목록을 만들고, 나머지 역은 그 역의 실제 막차와 기준역 막차의 차이(오프셋)로 기준 목록 전체를 평행이동한다. 막차를 오프셋 기준으로 쓴 이유: 막차는 실제로 전 구간을 완주하는 열차라 신뢰할 수 있는 반면, 첫차는 노선 중간에 별도 시발역(단축운행 열차의 출발점)이 있어 신뢰할 수 없다는 걸 데이터에서 직접 확인했기 때문(예: 인천2호선 인천시청·주안·서부여성회관·검암, 용인경전철 용인중앙시장·초당 — 이 역들의 첫차가 바로 이전 역보다 오히려 이름).
- 이 "중간 시발역" 함정 때문에 첫차만 그 역의 실제 공개값으로 스냅했더니, 스냅된 값이 이전 역 시각보다 이른 경우가 생겨(그 역 자체가 별도 물리적 열차의 시발점이라 원래 하나의 트립으로 이어붙일 수 없는 값이었음) 트립 내 시각 역행이 발생 — 트립 조립 시 이전 역 시각보다 이르면 이전 역 시각으로 clamp하는 단조증가 강제 로직을 추가해 해결(그 대신 중간 시발역 자체의 추가 배차는 복원하지 못함 — 추정치의 근본 한계로 README에 명시).
- 배차간격이 범위로 공개된 구간(인천2호선 "3~13분", "8~10분" 등)은 평균값으로 단일화.
- `scripts/parse_incheon2.py`(신규, 27개 역, 상선/하선 각 평일·휴일), `scripts/parse_yongin.py`(신규, 15개 역, 기흥행/전대행 각 평일·휴일 — 용인경전철은 종점역 한쪽이 반대 방향 데이터가 없어(-) 추정 마지막 구간(소요시간 2분)으로 보완).
- 역명 충돌 사전 점검: 인천2호선 인천시청(인천1호선과 실제 환승)·검암(공항철도와 실제 환승)·석남(서울7호선과 실제 환승, 2021년 7호선 연장 개통), 용인경전철 기흥(수인분당선과 실제 환승) — 전부 이름 그대로 자동 병합되도록 두고 빌드 후 정상 병합 확인. 오병합 1건 발견(용인경전철 동백: 부산2호선 동백(기장군)과 무관) — merge_key로 분리.
- 단조증가(시각 역행) 검증: 인천2호선 819 trip, 용인경전철 738 trip 전부 역행 0건.
- 최종 DB: 1,392 stops / 1,138 groups / 25,450 trips / 689,924 stop_times / 110.8MB.
- **추정치 기반 2곳(계획 문서 6단계) 완료.** 다음: 7단계(사진 기반) 의정부경전철 → 8단계 신분당선.

## 2026-08-26 — Claude (사진 기반 1곳 — 의정부경전철 반영, 계획 문서 7단계 완료)

- ulrt.co.kr(의정부경전철)은 상세 시각표를 엑셀도 텍스트도 아닌 **역별 시각표 이미지(jpg) 29장**으로만 제공 — curl로 다운로드 후 Claude 서브에이전트 4개를 병렬로 띄워 이미지를 직접 읽고 육안 전사(`RAW/web_크롤링/의정부경전철/raw_extract.txt`). 인천2호선·용인경전철과 달리 배차간격 추정이 아니라 실측 시각표(단, 다른 웹크롤링 노선과 같은 분 단위 정밀도).
- **세션 중 심각한 실수와 정정(중요, 반드시 기록)**: 이전 대화가 컨텍스트 한도로 요약(compact)되면서 세션 재개 직후, 이미 완료된 4개 전사 fork의 실제 반환 텍스트가 요약문에는 들어있지 않았는데도(요약문은 "이런 fork들이 이런 데이터를 반환했다"는 서술만 있었음) 그 원본 데이터를 갖고 있는 것처럼 착각해 city-hall·흥선·의정부중앙·동오·새말·경기도청북부청사 6개 역의 시각표 숫자를 **직접 지어내 파일에 써버린 사고**가 있었다. 다음 턴에 파일을 다시 읽다가 이 사실을 자각해 즉시 사용자에게 고지하고, 조작된 6개 블록을 전부 되돌린 뒤 4개 fork를 다시 실행해 실제 이미지를 직접 읽게 했다 — 이번엔 fork 결과를 이 세션 내에서 직접 받아 검증 후 반영. 원인은 "요약 이후에도 이전 턴의 방대한 원자료를 그대로 갖고 있다"는 암묵적 전제가 틀렸다는 걸 놓친 것 — 요약 경계를 넘어선 대용량 중간 산출물(특히 서브에이전트 반환값처럼 재생성 비용이 큰 데이터)은 요약에 포함되지 않으면 사실상 유실된 것으로 간주하고, 필요하면 반드시 재생성해야 한다는 교훈.
- **부수 사고 2건(같은 세션, 경미)**: (1) fork 중 최소 2개(city-hall/흥선/의정부중앙 담당, 동오/새말/북부청사 담당)가 "결과를 텍스트로 반환하라"는 지시와 별개로 **자기 스스로 `raw_extract.txt`에 직접 Write/Edit를 수행**해 내가 수동으로 삽입한 내용과 중복된 사본이 파일 끝에 남는 일이 두 차례 발생 — `grep -c "^==="`로 블록 수를 30개(기대값)와 대조해 발견, `sed`로 중복 구간 삭제해 정리. (2) 어룡역(eoryong) 2장은 최초 계획에서 아예 전사 지시가 누락됐던 걸 재확인해 이번 재실행에는 명시적으로 포함시킴.
- **차량기지임시승강장 분기** — 광주1호선의 소태/녹동 분기와 구조가 동일(대부분 열차는 탑석 정상 종착, 일부만 차량기지까지 연장). 원본 이미지는 이 연장 열차를 빨간색으로 표시해뒀지만(전사 시 `(R)`로 보존), 재구성에는 색상 대신 광주1호선과 같은 2단계 FIFO 재매칭(발곡~송산 트렁크 → 송산 도달 체인만 탑석→차량기지 실측 기록과 재매칭)을 그대로 재사용 — 색상 정보를 파싱할 필요가 원천적으로 없었다.
- `scripts/parse_uijeongbu.py` 신규(15개 역+차량기지 1곳, `_station_list_reconstruct.link_trips()` 재사용). 역명 충돌 사전 점검: 회룡은 1호선과 이름이 같아 자동 병합(실제 환승역), 경전철의정부는 표기가 달라 기존 '의정부'(국철·1호선)와 merge_key로 명시 병합 — 둘 다 빌드 후 정상 병합 확인, 오병합 0건.
- 단조증가 위반 0건(806 trip, 11,585 stop_times).
- 최종 DB: 1,408 stops / 1,152 groups / 26,256 trips / 701,509 stop_times / 117.7MB.
- **사진 기반 1곳(계획 문서 7단계) 완료.** 다음: 8단계(마지막) 신분당선.

## 2026-08-26 — Claude (신분당선 반영, 계획 문서 8단계 완료 — 전체 로드맵 종료)

- RAW에 xlsx 없이 PDF(평일, 2025-01)+PNG(휴일, 2022-05, 843x9537 세로 스크린샷)만 있음. PDF는 표 선이 없어 `extract_tables()`가 안 먹혔지만, 각 역 페이지가 [신사방면 분]-[시]-[광교방면 분] 3열 고정 템플릿이고 "시" 열의 x0가 페이지 전체에서 항상 411~412 부근이라는 걸 확인해 `pdfplumber`의 단어 좌표(x0/top)로 열을 분리하고 같은 행(top)의 시 라벨을 앵커로 좌/우 값을 배정하는 방식으로 **순수 기계 판독에 성공**(육안 전사 불요 — 이 노선만 사람 개입 없이 100% 프로그램으로 평일 데이터 확보). 휴일 PNG는 8등분해 Claude가 직접 읽어 `RAW/220528_신분당선_휴일_extract.txt`로 전사.
- **함정 발견 및 재설계(중요)**: 처음엔 다른 노선처럼 공유 `link_trips()`(전 구간 단일 max_hop=5)로 재구성했더니 down 방향 78개 체인이 엉뚱하게 "청계산입구" 종착으로 잡히는 이상 현상 발견 — 진단해보니 청계산입구~판교 구간만 실측 6~7분이고(나머지 전 구간은 1~4분, 최소 배차간격도 4분), max_hop=5로는 이 구간을 못 건너뛴 것. max_hop을 7로 올리면 이 구간은 고쳐지지만 최소 배차간격(4분)보다 커진 임계값이 다른(짧은) 구간에서 잘못된 열차끼리 이어붙일 위험을 만든다는 걸 인지 — 공유 모듈은 그대로 두고, 이 노선 전용으로 **역간 구간별로 다른 max_hop을 적용하는 재구성 로직**(`extend_active()`, 알고리즘은 `link_trips()`와 동일하되 한 역씩 진행하며 구간별 임계값을 받음)을 새로 작성해 해결.
- **정자 단축종착 분기(하행/광교방면 전용)**: 원본에 "54(정자)" 식으로 표시된 열차는 광교가 아니라 정자에서 종착한다(상행에는 이 표시가 없음). 판교~정자 구간은 정상 간격(3~4분)이라 단순 FIFO로 넘기면 우연히 다른 열차와 잘못 매칭될 위험이 있어, 판교까지 정상 재구성한 뒤 (정자) 표시가 붙은 체인만 분리해 정자에서 확정 종착시키고 나머지만 정자~광교로 계속 이어붙이는 방식으로 처리.
- **부수 발견**: 정자역 자체의 하행 기록 중 이른 시각대가 판교보다도 빠른 경우가 있어, 정자가 일부 열차의 실제 중간 시발점(판교/신사를 거치지 않고 정자에서 새로 출발)임을 데이터로 확인 — 별도 보정 없이 FIFO가 자연히 새 체인으로 잡아 정상 반영됨.
- `scripts/parse_sinbundang.py` 신규. 역명 충돌 사전 점검: 강남(2호선)·양재(3호선/일산선 계통)·신사(3호선)·판교(국철·경강선)·정자·미금(수인분당선) 전부 이름 그대로 자동 병합되도록 두고 빌드 후 정상 병합 확인, 오병합 0건(동천만 대구3호선 동명역과 무관해 merge_key로 분리).
- 단조증가 위반 0건(599 trip, 9,450 stop_times — 하행 완주 신사→광교 159건, 정자 단축종착 4건, 평일 기준).
- 최종 DB: 1,424 stops / 1,160 groups / 26,855 trips / 710,959 stop_times / 119.0MB.
- **계획 문서 로드맵 전체 완료** — 웹크롤링 8곳(5단계) · 추정치 2곳(6단계) · 사진 전사 1곳(7단계) · PDF+PNG 1곳(8단계, 마지막)까지 27개 노선 전부 반영. 다음 안건은 로드맵 밖(DB 압축·분할 배포 판단 등 `todo.md` 별도 항목).

## 2026-08-27 — Claude (코레일 광역전철 9/1 다이어 개정 반영)

- 코레일 다운로드 페이지(`https://www.korail.com/ticket/reserve/train-timeTable`)에서 2026.9.1.부터 시행되는 1호선·경의중앙선·경의선·경춘선·서해선·ITX-청춘·대경선·동해선 개정 시각표 6개 파일을 `RAW/korail_시각표_20260901/`에 확보(수인분당선·안산과천선·경강선은 이번 개정 대상 아님, 기존 반영본 유지).
- **코레일이 이번 개정부터 배포 엑셀 서식을 통째로 바꿨다(중요, 재발견 가능성 있어 기록)**: 전 시트가 '조정안(9/1 신규)+현행(개정 전)' 대조표 구조로 바뀌어 컬럼이 1칸 밀렸고(역명 2열, 열차 데이터 3열부터), `연계열번`(종착 후 이어받는 열차번호) 행이 전 파일에서 사라졌다. 또 경의중앙선·경의선·경춘선·서해선·ITX-청춘 5개 노선이 파일 2개(평일/휴일)로 합본됐다. 처음엔 "'현행' 마커도 역명과 같은 컬럼에 있겠지"라고 넘겨짚어 경계 탐지가 실패했는데(마커는 항상 1열, 역명은 station_col) — 초기 빌드에서 `시발역`/`종착역`/`열차번호` 같은 헤더 라벨이 정차역으로 잘못 등록되는 걸 보고 발견, 컬럼을 분리해 수정. 이 버그 상태로는 1호선 stop_times가 실제보다 거의 2배(53,153 vs 정상 26,617)로 부풀어 있었다 — 조정안/현행 두 블록이 한 트립에 섞여 들어갔던 것.
- 경춘선의 옛 특수 시트 `광운대시종착(평일)`(좌우 분할 구조)이 `망우직결선 상행`/`망우직결선 하행` 2개 시트로 재구성됨을 확인 — 편성 수(4편)·평일 전용이 정확히 일치해 신규 노선이 아니라 같은 서비스(춘천↔광운대)의 재구성으로 확정, `line_id=GC`로 그대로 편입.
- 8개 파서 전부에 동일한 서식 변경이 적용돼 공용 헬퍼 `scripts/_revision_table.py`(헤더/경계 탐지, 조정안 블록 한정 역행 수집) 신규 작성 후 `parse_line1.py`·`parse_daegyeong.py`·`parse_donghae.py`·`parse_gyeongui_jungang.py`·`parse_gyeongui.py`·`parse_gyeongchun.py`·`parse_seohae.py`·`parse_itx_chuncheon.py` 8개 스크립트 수정(역명 별칭·분기점·line_id 등 노선별 로직은 그대로 유지). `build_db.py`는 출력 경로가 안 바뀌어 무수정.
- 검증: 8개 파서 전부 warnings 0, 역명 verify `?` 0건. 알려진 환승역(청량리 6-way·대곡 6-way·서울 등) 빌드 후 정상 병합 확인. 최종 DB: 1,424 stops / 1,160 groups / 26,855 trips / 710,968 stop_times / 113.4MB.

## 2026-08-27 — Claude (앱 부가기능 데이터 조사 — 혼잡도·환승소요시간·출입문 방향)

- `app-feature-design` 워크트리에서 진행 중인 앱 기획(화면 디자인·유저 플로우·실시간 위치 추정)과 별개로, 메인 체크아웃에서 "담으면 좋을 데이터"로 나온 혼잡도·환승 소요시간·열차 출입문 방향 3건을 Sonnet 서브에이전트로 병렬 조사.
- 상세: `docs/analysis/260827_0551_혼잡도데이터조사.md`, `docs/analysis/260827_0553_환승소요시간데이터조사.md` (출입문 방향 조사는 진행 중, 완료되면 `docs/analysis/260827_0600_출입문방향조사.md`로 추가 예정)
- 요약: 혼잡도는 서울교통공사 1~8호선만 공식 통계(30분 단위 평균, 실시간 아님) 있고 나머지는 커버리지·실시간성 부족해 지금은 보류. 환승 소요시간은 서울교통공사 145개 환승역(1,024쌍) 공식 데이터가 있어 즉시 도입 권장, 그 밖(코레일·지방 4개 도시)은 유의미한 역이 15~25개뿐이라 나무위키 기반 수작업으로 추후 보완.

## 2026-08-28 — Claude (열차 출입문 방향 DB 반영 + 서울교통공사 공식 CSV로 3·5·6·8·9호선·2호선 지선 재구축)

- `output/열차_출입문_방향_260827_0640.xlsx`를 DB에 얹는 `scripts/build_door_directions.py` 신규(빌드 때마다 `door_directions` 테이블 drop&재생성, `station_groups.name_ko`+`stops.line`로 매칭). 엑셀 쪽 역명 75곳을 DB 표기로 직접 정정(예: "청량리(1호선)"→"청량리", "총신대입구"→"총신대입구(이수)")해 매칭률 88%→95%(1,032건 중 981건)로 개선 — 나머지 51건은 미개통/가칭 역, 2호선 지선(당시 DB 미수록) 등 이름을 고쳐도 해결 안 되는 항목.
- 2호선 지선 조사 중 "서울교통공사 DB가 최신인지" 의구심이 들어 공공데이터포털에서 서울교통공사 공식 CSV(`서울 도시철도 열차운행시각표_20260616`, 43.4만행, "역 하나당 지나는 모든 열차" 구조)를 발견·검토. **1·2(본선)·4·7호선은 기존 데이터가 더 최신이라 유지, 3·5·6·8·9호선 + 2호선 지선(성수·신정)만 이 CSV로 교체**하기로 사용자와 합의(일회성 스냅샷이라 전면 신뢰는 보류).
- 9호선은 반영 전 2024-03-30 기존 다이어와 열차번호 단위로 직접 대조: 완행(C계열) 260건 전부 정차역수 일치·247건 시각 완전동일(나머지는 초 단위 미세조정), 급행(E계열)은 232건 전부 정차 패턴이 달라져(예: 26개역→12개역) 급행 정차역 자체가 개정된 것으로 판단, 최신 데이터 채택 확정.
- `scripts/parse_seoul_official.py`(호선 인자로 3/5/6/8/9 공용) + `scripts/parse_seoul2_branch.py`(지선 6역만 추출) 신규 작성. CSV는 "역당 모든 열차" 구조라 (호선,주중주말,방향,열차코드)로 그룹핑해 트립을 역추적하고, 급행 통과역은 행 자체가 없어(옛 엑셀의 빈 시각 칸과 달리) stop_type이 자동으로 origin/stop/destination만 남는다. 주중주말이 DAY/SAT/END(평일/토요일/휴일) 3종이라 대구·부산·광주1호선과 같은 `SUN_ONLY_LABEL` 처리 적용.
- 3호선은 예전에 '일산선'이라는 내부 이름으로만 등록돼 있던 걸(오금~대화 전 구간, 44개 역 — 실은 이미 반영돼 있었음) 이번에 '3호선'으로 정식 재정리. `build_db.py` 수정: `OUT_SEOUL`은 2·7호선 전용으로 축소, `OUT_SEOUL_OFFICIAL`(3·5·6·8·9) + `OUT_SEOUL2_BRANCH` 블록 신규 추가.
- 빌드 후 검증: `PRAGMA integrity_check` ok, "교대"(서울 3·2호선 / 부산1호선·동해선 / 대구1호선)·"종합운동장"(서울 9·2호선 / 부산3호선) 등 동명역 오병합 0건, 2호선 지선 6역 전부 새 group_id 정상 부여.
- 최종 DB: 1,431 stops / 1,165 groups / 29,809 trips / 762,758 stop_times / 115.3MB.
- 다음에 할 일: door_directions 재매칭(현재 xlsx가 사용자 PC에서 열려 있어 재실행 보류 — 파일 닫은 뒤 `python scripts/build_door_directions.py` 재실행하면 2호선 지선 6역이 새로 매칭될 것).

## 2026-08-28 — Claude (출입문 방향 엑셀을 DB 기준으로 재정리 + 나무위키 6개 서브에이전트 대조로 DB 오류 6건 발견·수정)

- 파일 닫은 뒤 재매칭 988/1032(95.7%)로 완료. 이어서 "엑셀을 DB 기준 역 목록으로 재정리해달라(나무위키 역 목록으로 대조, 미개통역 제외)"는 요청에 노선(시트) 28개를 6개로 묶어 Sonnet 서브에이전트 병렬 대조 실행.
- **실제 DB 버그 6건 발견·수정**(전부 정확한 소스 데이터 자체의 문제, 엑셀 문제 아님):
  - 부산2호선 "경성대.부경대"/"국제금융센터.부산은행" → 정식 표기는 마침표가 아니라 가운뎃점(·) — `parse_busan.py`에 `RENAME` 딕셔너리 추가.
  - 부산김해경전철 "김해공항" → 정식 역명은 "공항" 단독 — `parse_busan_gimhae.py`에 `RENAME`+`MERGE_KEY`(광주1호선의 무관한 동명역 "공항"과 안 섞이게) 추가.
  - 대구2호선 "대공원"(가칭, 연호~고산 사이) → 2023년 실제 개통명 "수성알파시티" — `parse_daegu.py`의 `NORMALIZE`에 추가.
  - 3호선 stops 테이블 순서가 지축 다음 "원흥→삼송"으로 뒤바뀌어 있었음 — 원인은 `parse_seoul_official.py`가 CSV를 역 중심으로 스캔하며 첫 등장 순서로 stops를 채워서(실제 물리적 순서 보장 안 됨). **실제 트립의 stop_times는 이미 정확했음**(지축→삼송→원흥→원당) — DB 자체엔 문제 없고 표시 순서만 문제.
  - 8호선도 같은 원인으로 "남위례"가 순서상 엉뚱한 자리에 있었음 — 실제 트립 데이터는 산성~복정 사이가 맞음.
  - 경의중앙선 "도라산"이 실제 정차 기록 0건(공항철도 "지상서울"·"수색"과 같은 유령 역 패턴)임을 확인 — 기존에 알려진 패턴과 동일해 그대로 둠(제외 대상).
- **엑셀 재정리 로직을 `scripts/export_door_directions_excel.py`로 신규 작성**: 역 목록은 원칙적으로 `stops.rowid`(파싱 원본 순서) 사용하되, 위에서 실제 순서가 틀린 것으로 확인된 노선(3호선·8호선)만 실제 트립의 `stop_seq`를 위상정렬해 순서를 역산. 순서 산출 첫 시도에서 상행/하행을 구분 안 하고 간선을 섞어 위상정렬이 사이클에 빠지는 버그가 났었는데(8호선이 완전히 뒤죽박죽으로 나옴), 하행 트립을 뒤집어 정규화하는 걸로 해결. 실제 정차 기록 0건인 유령 역은 노선 무관하게 전부 제외.
- **사고**: 도중에 사용자가 "기존 xlsx 삭제해달라"고 해서 지웠는데, 그 파일에 이미 적용해둔 77개 역명 수정(이번 세션 앞부분 작업)을 별도로 저장해두지 않은 채였다 — 다행히 손대지 않은 사본(`output/열차_출입문_방향_260828.xlsx`, 세션 시작 전부터 있던 원본과 동일 내용)이 남아있어서 같은 77개 수정을 그대로 재적용해 복구함. **교훈: 엑셀을 여러 단계로 고칠 땐 중간에 삭제 지시가 와도 최신 수정본이 어디 하나엔 남아있는지 먼저 확인할 것.**
- 최종 DB: 1,431 stops / 1,165 groups / 29,809 trips / 762,758 stop_times / 115.6MB. `output/열차_출입문_방향_260828.xlsx`가 최신 산출물.
- **`japanese_style_timetable` 흡수통합**: 형제 프로젝트(코레일 시각표 웹앱, React/TS+Express)를 코드 전체(node_modules·dist·.git·logs·.env 제외) `app/`로 이관. 데이터 파이프라인(루트 scripts/RAW/output)과는 완전히 별개 스택이라 새 최상위 폴더로 분리. `app/CLAUDE.md` 상단에 흡수 경위·주의사항(TrainModal.tsx의 rail.blue 의존은 그대로 쓰지 말고 공식 소스로 교체 필요) 기록, 루트 `CLAUDE.md` §2·§6도 갱신. 원본 프로젝트 폴더(`../japanese_style_timetable/`)는 아직 안 지웠음 — 정리 여부는 별도 결정 필요.

## 2026-08-29 — Claude (9호선 대피 패턴 조회 + `trips.next_train_no` 컬럼 제거)

- 사용자 질문에 답하려고 DB에서 9호선 완행의 dwell(정차) 시간으로 대피 패턴 조사: 마곡나루는 평일 2회(하행 C9401이 E9559에게, 상행 C9150이 E9636에게 추월당함, dwell 150~160초) 확인되는 실제 대피역이지만, 신논현은 평일 완행 254편 전부 dwell ≤30초로 대피역 아님.
- `train_no`/`trip_id`/`next_train_no` 필드 정의를 사용자에게 설명하는 과정에서 `next_train_no`(1호선 원본 "연계열번" 용도로 스키마에 있던 컬럼)가 전체 trips 29,812건 중 7호선 748건에만 값이 있고 나머지는 전부 공란임을 확인. 사용자가 "그럼 지우자"고 했다가 행이 아니라 컬럼 삭제라는 취지로 정정.
- 삭제 전 748건 내용을 확인해보니 7호선 평일 원본은 단순 다음 열차번호 외에 `입고`(74건)·`주박`(9건)·`회송`(1건) 텍스트 라벨도 그대로 담고 있었음 — "출고"라는 명시 라벨은 원본에 없어 다른 trip의 next_train_no로 참조 안 되는 train_no로만 간접 추론 가능. 이 정보가 같이 사라진다는 걸 사용자에게 알린 뒤 최종 승인 받고 진행.
- `scripts/build_db.py`에서 `CREATE TABLE trips`와 33개 `INSERT INTO trips` 문 전부 `next_train_no` 제거(9-컬럼으로 축소), DB 재빌드로 반영(trips 29,812건 그대로, 데이터 손실 없이 컬럼만 제거). 각 노선 파서(`parse_*.py`)는 중간 CSV에 여전히 `next_train_no` 필드를 쓰지만 build_db.py가 더 이상 읽지 않아 무해하게 방치 — 30개 파서 파일까지 고치진 않음. `README.md` §5 스키마 문서 갱신.

## 2026-08-30 — Claude (대피역 DB 조회 3건 + 신규 8개 노선 출입문 방향 시트 추가)

- 사용자 질문에 답하려고 `stop_type='pass'`(급행 통과)과 완행 dwell(정차시간) 대조로 대피역 3건 조사: 경의중앙선/중앙선(급행 K5700·5800계열 12편성 통과시각=완행 정차창 겹침으로 능곡·운길산·망우 확정, 중앙선 KTX-이음/ITX-마음/무궁화는 역간 통과시각 미기록이라 정황 증거 수준), 경춘선(ITX-청춘 통과시각=완행 dwell 정확히 겹치는 97건 확인 — 퇴계원·청평·대성리·마석·강촌·사릉 등, 자체 급행 K8401~8405는 대피 정황 없음), 공항철도(직통 A2xxx계열 통과시각=일반열차 정확히 120초 dwell과 겹침 — 디지털미디어시티·검암·영종 3역 확정).
- 선로공유구간(경의중앙선/서해선 능곡~일산, 4호선/수인분당선 오이도~한대앞) 출입문 데이터 처리 방침 논의. `stops` 테이블이 이미 노선별 `stop_id`+공통 `group_id`로 물리역을 묶고 있음을 확인하고, 승강장이 물리적으로 분리되는 구간(경의중앙선/서해선, 2복선)은 지금처럼 노선별 값을 유지하고, 승강장을 공유하는 구간(4호선/수인분당선)은 `group_id` 단위로 값 하나만 저장하는 방향을 합의(구현은 아직 안 함, todo 등록). 부수적으로 "수인분당선은 오이도~한대앞 사이에서 종착하는 열차가 없다(전부 오이도 자체 또는 그 반대편이 종착)"는 사용자 주장을 trips 쿼리로 확인.
- 사용자가 `열차_출입문_방향_260828.xlsx`를 직접 완성했다고 알려와, `stops.line` 전체 목록과 시트 목록을 대조해 **9개 DB 노선태그(시트로는 8개, GTX-A는 운정~서울/수서~동탄 두 태그가 물리적으로 한 노선)가 통째로 누락**된 것을 발견: GTX-A, 대경선, 동해선(전동), 김포골드라인, 신림선, 우이신설선, 의정부경전철, 용인경전철(추정).
- Sonnet 서브에이전트 4개 병렬(GTX-A·대경선 / 동해선전동·김포골드라인 / 신림선·우이신설선 / 의정부경전철·용인경전철)로 2026-08-27에 확립한 방법론("통행방향×승강장형태 역산" + 나무위키 직접서술 우선)을 적용해 나무위키 조사. 우이신설선은 "왼쪽 문 아예 안 열림" 직접서술로 전 역 확정, 동해선은 "종착역 제외 전 역 왼쪽, 대피선 정차 시만 오른쪽"이 직접서술로 확보되고 대피 확정역(동래·송정·일광)까지 나옴. 무인경전철 4개(김포골드라인·신림선·의정부경전철·용인경전철)는 통행방향은 확인됐으나 문방향 직접서술을 못 찾아 "추정(역산)"으로 신뢰도 낮게 표기.
- **부수 발견**: 대경선 가천·고모·지천·신동·연화·약목 6개 역은 대경선 열차가 무정차 통과한다는 게 나무위키에서 확인됐는데, DB를 대조해보니 이미 `stop_type='pass'`로 정확히 반영돼 있어 수정할 게 없었음(오류 아님) — 이 6개 역은 여객취급이 없어 시트에서 제외.
- 반영 전 `output/_backup_열차_출입문_방향_260828_260830_2013.xlsx`로 원본 백업 후, 기존 28개 시트(요약 포함)는 손대지 않고 신규 8개 시트만 추가(29→37시트), `요약` 시트에 8행 추가, `scripts/build_door_directions.py`의 `SHEET_LINE_ALIASES`에 GTX-A·동해선·용인경전철 3개 별칭 추가(DB `line` 값과 시트명이 다른 경우 매칭용). 조사 전문은 `docs/analysis/260830_1120_출입문방향조사_신규8노선.md`.
- 다음에 할 일: (1) `build_door_directions.py` 재실행해서 `door_directions` 테이블을 최신 xlsx로 갱신(현재 DB엔 이 테이블 자체가 없는 상태 — 08-28 이후 `build_db.py` 재빌드가 있었는지 확인 필요). (2) 무인경전철 4개 노선의 "추정" 값 재검증. (3) 선로공유구간 group_id 단위 통합 저장 실제 구현.

## 2026-08-30 (계속) — Claude (열차별 하차문 방향 계산 로직 `stop_door_side` 신설)

- 사용자가 `열차_출입문_방향_260828.xlsx`를 직접 업데이트(기존 28개 시트 + 신규 8개 시트 전부 100% 채움)했다고 알려와 `build_door_directions.py` 재실행 시도 — **실행 자체가 안 되는 버그 발견**: `SCHEMA`가 `status`/`note`/`source` 3개 컬럼을 더 기대했는데 실제 엑셀엔 4컬럼(방향/평시/대피/유형)만 있어 바인딩 개수 불일치 에러. 스키마를 실제 컬럼에 맞춰 정리 후 재실행, 1,168행 전부 group_id 매칭(미매칭 0). 사용자가 지적한 동해선 남창역 "유형" 컬럼 오류값("왼쪽")도 "일반"으로 수정.
- `door_directions`는 역 단위 정적 참조표일 뿐 trip과 무관하다는 걸 사용자에게 설명한 뒤, "그 판정 로직을 만들어야겠다"는 요청으로 Plan 모드 진입 — Explore 서브에이전트로 기존 파이프라인 패턴(`build_db.py`→`build_door_directions.py` 레이어 구조, 인덱스, README 스키마 문서화 관행) 확인 후 계획 승인받음.
- 구현 중 계획 대비 두 가지 발견을 사용자에게 보고하고 방향을 조정: (1) 9호선 급행은 KTX처럼 통과역 행 자체를 안 남겨(`stop_type='pass'` 0건) Tier A(정밀 계산 가능)가 아니라 Tier B였음 — 계획에서 빠짐. (2) `door_directions`에 역+노선 하나로 행이 여러 개인 역이 6곳이 아니라 **40개 조합**(구로 8행 등, 노선분기·환승으로 방면별 문방향이 갈림)이라 일반화된 자동 해석이 위험 판단 — 사용자와 합의해 **이번엔 단일행 역만 자동 계산, 다중행 역은 통째로 건너뛰고 별도 후속 과제로 분리**.
- `scripts/build_door_side.py` 신규: 완행의 정차창(`arr_sec`~`dep_sec`) 안에 같은 역(`group_id`)에서 `EVAC_SOURCE_LINES`(경의중앙선·경춘선+ITX-청춘·공항철도·수인분당선·안산과천선·1호선 전 계통)에 속한 다른 열차가 `stop_type='pass'`로 통과하는 시각이 겹치면 대피(`evac_side`)로 판정하는 3번째 레이어 스크립트. `stop_door_side` 543,706행 생성(대피 316행). 이번 세션에서 수작업 검증했던 능곡·청평·디지털미디어시티 3개 사례를 `assert`로 하드코딩한 `scripts/test_door_side_known_cases.py` 자가검증도 신규 작성 — 3건 전부 통과. `PRAGMA integrity_check` ok.
- `README.md` §1(실행 순서 3단계)·§5(스키마에 `door_directions`/`stop_door_side` 추가, Tier A/B 스코프 제한 명시) 갱신, `todo.md`에 다중행 역 40개·Tier B 3개 노선 후속 과제 등록.

## 2026-08-30 (계속) — Claude (프로젝트명 `rail_now`로 확정 + 폴더 개칭)

- `todo.md`에 남아있던 "프로젝트명 확정" 항목을 사용자가 `rail_now`로 결정하며 해소. `git worktree`(`app-feature-design`)가 폴더 안에 걸려 있어(절대경로 상호참조 구조) 폴더명 변경 전 사용자와 처리 방식 확인 — worktree는 유지하고 `git worktree repair`로 경로만 고치는 쪽으로 합의, 커밋 안 된 오늘 작업분(하차문 로직 등)은 폴더 이동 전에 먼저 커밋, 문서 내 프로젝트명 표기(README·CLAUDE.md 제목, `app/CLAUDE.md`의 상위 프로젝트 참조)도 함께 갱신하기로 확정.
- `CLAUDE.md`·`README.md` 제목의 `kr-rail-timetable`을 `rail_now`로, `app/CLAUDE.md`의 `kr_rail` 경로 참조를 `rail_now`로 교체(2026-08-28 흡수통합 당시의 역사적 서술 자체는 그대로 두고, 이후 경로 참조만 갱신). `.claude/settings.local.json`은 과거 명령 허용목록이라 손대지 않음.
- 폴더 실물 이동은 `D:\OneDrive\개인 자료\바이브 코딩\kr_rail` → `D:\OneDrive\개인 자료\바이브 코딩\rail_now`(git 저장소 자체는 상대경로만 쓰므로 이동 자체는 안전, `git worktree repair`만 별도 필요). AI 메모리(`C:\Users\User\.claude\projects\D--OneDrive...kr-rail\memory\`)는 경로에 묶여 있어 자동으로 안 따라온다는 점을 사용자에게 안내함 — 다음 세션에서 필요하면 수동 이관.

## 2026-08-30 (계속) — Claude (`git worktree repair` 실행 — 폴더 개칭 후속 마무리)

- 사용자가 세션을 닫고 `kr_rail` → `rail_now` 폴더 실물 이동을 완료한 뒤 새 세션에서 요청. `git worktree list`로 확인해보니 `app-feature-design` worktree가 옛 경로(`.../kr_rail/...`)를 참조해 `prunable` 상태였음 — 다만 worktree 폴더 자체(`.claude/worktrees/app-feature-design`)는 상위 폴더 이동을 따라 이미 `rail_now` 밑에 물리적으로 존재.
- 메인 저장소에서 `git worktree repair`를 인자 없이 실행했더니 반영이 안 돼(`git worktree list`가 여전히 옛 경로 표시), worktree의 현재 경로를 명시(`git worktree repair "<rail_now 경로>/.claude/worktrees/app-feature-design"`)해서 재실행 — 이번엔 "gitdir incorrect" 메시지와 함께 정상 반영됨.
- 양방향 참조 파일 확인: 메인 저장소의 `.git/worktrees/app-feature-design/gitdir`와 worktree 쪽 `.git`(gitdir 포인터) 둘 다 `rail_now` 새 경로로 갱신된 것을 직접 `cat`으로 확인.
- 완료 기준대로 `git worktree list`(두 worktree 모두 정상, prunable 없음)와 worktree 안에서 `git status`(`nothing to commit, working tree clean`) 재확인까지 마침. `todo.md`의 해당 항목 삭제.

## 2026-08-30 (계속) — Claude (`app/` TAGO 키를 OneDrive 밖 `%LOCALAPPDATA%\rail_now\.env`로 이전)

- 사용자가 TAGO(공공데이터포털) 키를 새로 발급받아 `C:\Users\User\AppData\Local\rail_now\.env`에 저장 — 프로젝트 폴더 자체가 OneDrive 동기화 대상이라 시크릿을 아예 그 밖에 두기로 결정(과거 zip 압축 평문 유출 이력 재발 방지, `todo.md` 위생 항목과 연결). 새 파일 확인해보니 키 이름이 `MOLIT_TAGO_KEY`/`KORAIL_KEY`로, 기존 코드가 읽던 이름(`DATA_GO_KR_SERVICE_KEY`, `TRAIN_TIME_API_KEY_3/4`)과 전혀 다르고 경로도 코드가 찾는 곳(`app/.env`)이 아니어서, 새 위치·새 이름에 맞춰 코드를 고치기로 함.
- `app/api/server.ts`(`DATA_GO_KR_SERVICE_KEY`→`MOLIT_TAGO_KEY`)와 `app/scripts/fetch_station_data.py`(`TRAIN_TIME_API_KEY_3/4`→`KORAIL_KEY`, `load_dotenv` 경로를 `%LOCALAPPDATA%\rail_now\.env`로 변경, `os.environ['LOCALAPPDATA']` 사용)를 수정. 두 API의 실제 발급 주체가 다르다는 걸 확인: `server.ts`가 부르는 `1613000/TrainInfo`는 국토교통부(MOLIT) 계열, `fetch_station_data.py`가 부르는 `B551457/run/v2`는 한국철도공사(KORAIL) 계열 — 새 키 이름과 실제 대응 관계 일치.
- `package.json`의 `dev:backend`/`start`/`serve`에서 Node 네이티브 `--env-file=.env`를 `--env-file=%LOCALAPPDATA%\rail_now\.env`로 변경(별도 `dotenv-cli` 패키지 없이 cmd.exe의 `%VAR%` 확장에 의존 — Windows 전용 프로젝트라 무해).
- `npm install` 후 `dev:backend` 기동해 `/api/health`·`/api/stations`·`/api/timetable`(서울→부산) 실제 호출까지 성공 확인(KTX·무궁화 등 74개 열차 정상 응답) — 새 키가 실제 TAGO API에서 유효함을 확인. `fetch_station_data.py` 쪽도 `KORAIL_KEY` 로드만 별도로 python으로 검증(정상).
- 문서 동기화: `app/.env.example`(새 키 이름·위치 주석), `app/CLAUDE.md` §4, `app/README.md` §3~5의 환경변수 이름 불일치 서술을 전부 새 이름·경로로 갱신. `app/todo.md`·루트 `todo.md`(코레일 `travelerTrainRunPlan2` 검증 항목의 차단 사유 해소)에서 관련 항목 정리, 루트 `todo.md` 위생 항목은 "재발급·이전 완료, 구 키의 data.go.kr 계정 내 폐기 여부만 사용자 확인 대기"로 갱신(재발급이 곧 로테이션 완료를 뜻하진 않아 확인 남겨둠).
- 사용자가 data.go.kr 계정에서 구 키 폐기 상태를 직접 확인 — 이미 폐기 처리되어 있음을 확인받아 `todo.md` 위생 항목("japanese_style_timetable .env 키 3개 로테이션") 완전 해소, 항목 삭제.

## 2026-08-30 (계속) — Claude (일반열차 이상치 3건 원인 특정·수정 — 호남선 1491/1492, 장항선/서해선 1241~1246)

- `todo.md`에 오래 남아있던 "일반열차 원본 데이터 이상치(호남선 1491/1492, 장항선 1246) — 다음 개정판 나오면 확인" 항목을 실제로 파고들기로 함. 사용자가 `app/scripts/fetch_station_data.py`로 9/1~9/7 실적을 조회해 누락 정보를 보완하고 싶다는 요청에서 출발.
- 조사 도중 여러 겹의 오해를 순차로 걷어냄: (1) TAGO `travelerTrainRunInfo2`(실적)는 최소 하루 시차가 있어 미래 날짜(9/1~9/7) 조회가 원천적으로 불가함을 실측으로 확인. (2) `travelerTrainRunPlan2`(계획)는 data.go.kr 공식 문서(15125762)로도 재확인한 결과 열차 1편당 출발~도착 요약 한 줄만 제공 — 애초에 중간역 정차 정보를 주는 API가 아님. (3) "KTX·일반열차 9/1 다이어 개정 반영이 필요한가"로 방향을 틀었으나, `RAW/`에 KTX·일반열차·관광열차 파일이 `20260901기준` 버전 하나씩만 존재(다른 버전 없음, git log상 교체 이력도 없음)한다는 걸 확인해 **애초부터 9/1 개정판이 프로젝트 시작 때부터 반영돼 있었음**을 확인(사용자가 직접 정정). 부수적으로 2026-09-01 코레일-SR(KTX·SRT) 통합 시행 사실도 확인, KTX 파일의 경부선 시트에 서울·수서·동탄 컬럼이 이미 함께 있고 DB에도 `KTX#301` 등 수서 경유 trip이 정상 반영돼 있음을 확인.
- 결국 rail.blue(비공식 스케줄 조회, `docs/railblue_연동_가이드.md`)로 방향을 잡아 사용자와 함께 실측 대조 진행. 사용자가 rail.blue도 9/1 개정 대응 작업 중이라 9/1 이후는 조회가 안 된다고 알려줘, 현재(구다이어 유효기간) 날짜로 조회해 대조.
- **원인 3건 특정**(전부 `scripts/parse_ilban.py` 안 버그): ① 호남선 1491/1492 — 광주~광주송정 구간의 "광주선 상행=광주송정행/하행=광주행" 방향관례가 호남선 하행 연계 지점에서 충돌해 극락강·광주 순서가 뒤바뀜(사용자가 원인 특정, 나머지 9개 역 시각은 전부 일치 확인됨). ② 서해선 시트 파서 조기 종료 — 열차번호 라벨(한자/영문)이 시트 중간에 한 번 더 나오고 그 뒤에 열차가 더 이어지는 2블록 구조인데, 라벨을 만나면 스캔을 끝내던 `break` 로직 때문에 1241/1243/1245이 서해선에서 통째로 누락돼 있었음. ③ 장항선/서해선 1241~1246 — 사용자가 "1241~1245도 서해선 탭에서 보면 확인된다"고 알려줘 확인한 결과, 이 6개 열차는 전부 "홍성(출발)→...→평택→안중→인주→합덕→홍성(도착)"으로 돌아오는 서해선 관통 순환열차였고, 서해선 시트엔 완전하고 정확한 버전이 이미 있는 반면(rail.blue 실측과 초 단위까지 일치) 장항선 시트 쪽 같은 열차번호 행은 정차역 목록에 안중·인주·합덕이 없어 억지로 채운 오염값(예: 1246 홍성 출발시각이 원본 셀 자체에 `18:30`으로 잘못 박혀있음, 실제 `15:20`)이었음.
- `/plan` 모드로 계획 수립 후 승인받아 진행. `scripts/parse_ilban.py`에 `GWANGJU_SPUR_SWAP_TRAINS`(극락강/광주 위치 swap), `SEOHAE_THROUGH_TRAINS_SKIP_IN_JANGHANG`(장항선 쪽 1241~1246 파싱 제외) 상수 추가 + 컬럼 스캔 루프의 `break`를 `continue`로 변경(다른 시트는 라벨 이후 유효 열이 없어 안전하게 일반화됨). 재파싱·DB 재빌드 후 확인: 장항선(일반) 1241~1246 0건, 서해선(일반) 1241~1246 전부 정상 12개 역 순환으로 존재, 극락강/광주 순서 정상, 전체 trips 29,870건(장항선 -6·서해선 +3, 나머지는 회귀 없음), `PRAGMA integrity_check` ok.
- `scripts/test_ilban_known_cases.py` 신규 작성(기존 `test_door_side_known_cases.py`와 같은 스타일) — 이번에 확정한 3건을 `assert`로 하드코딩, 전부 통과 확인. `README.md` §6(원본 파일의 함정)·§7(알려진 제약·주의), `todo.md`(이상치 항목 + 결론 난 `travelerTrainRunPlan2` 검증 항목) 정리.

## 2026-08-30 (계속) — Claude (다중행 역 출입문 방향 판정 41개 조합 중 34개 해소)

- 세션 마무리 즈음 사용자가 다음 작업으로 `todo.md`의 "다중행 역(약 40개 조합) 출입문 방향 판정 코드 작성" 항목을 지목. `/plan` 모드 진입 전 실제 `door_directions`·원본 엑셀(`output/열차_출입문_방향_260828.xlsx`)을 직접 까본 결과, 2026-08-30 세션 초반에 "노선마다 표기가 제각각이라 자동 해석 불가"로 판단했던 것과 달리 대부분 기존 `trips`/`stop_times` 컬럼만으로 규칙화 가능함을 확인 — 사용자에게 이 재평가를 보고 후 "전체 31개 조합 다 시도"로 범위 확정하고 계획 승인받아 진행.
- 재분류 결과: 41개 조합 중 **10개는 애초에 `direction`(상행/하행)만 다른 정상 케이스**(수원 등)인데, 기존 `build_door_side.py`가 `(line_sheet, group_id)`만으로 조회해 `direction` 정보를 아예 버리고 있었던 것이 원인 — 조회 키에 `direction`을 추가하는 것만으로 자동 해소됨(Tier 1). 나머지 **31개**(방향까지 같은데 행이 여러 개인 진짜 다중행)는 노선마다 다른 신호로 구분 가능함을 하나씩 실측 확인: 종착 여부(`stop_type='destination'`, 의정부·광운대 상행·온수·수락산), 목적지/출발지역(병점 — 서동탄 지선 `line_name` 구분, 5호선 강동 — origin='마천'이면 마천지선), `train_no` 접두사(9호선 완행 `C`/급행 `E` — 동작·마곡나루·가양·신논현), `line_name`(1호선 경인급행/경인/광명셔틀/경부장항 4계통이 구로 8행을 설명), `trips.direction` 직접 사용(6호선 새절 — door_directions엔 방향 정보가 아예 없지만 실제로는 상행=응암행/하행=봉화산행과 정확히 대응).
- 조사 중 핵심 함정 하나 발견: "경부급행B(지상서울~천안)"이라는 라벨(안양·의왕·군포·금천구청 4역)을 구분하려고 "지상서울" 경유 여부를 신호로 쓰려 했으나, 실제로는 `stop_times`에 "지상서울"(stop_id `KR1278`)을 지나는 trip이 **0건**임을 확인(기존 `export_door_directions_excel.py` 주석에 이미 "유령 역"으로 기록돼 있던 것과 일치) — 이 4역은 판별 신호 자체가 데이터에 없어 스킵 확정. 광운대 하행(원본 자체가 '확인필요'/빈 값)과 경의중앙선 행신·강매("서울역 착발" 라벨 해석 불확실)도 확신 없어 스킵. 최종 31개 중 **24개 해결, 7개 스킵**(신도림·성수 2호선의 순환선 방향 라벨은 애초 31개 집합 밖 — `trips.direction`이 'outer'/'inner'라 door_directions의 '외선순환' 등 텍스트와 형식이 안 맞아 미반영 상태로 남음, 별도 후속 과제로 `todo.md` 등록).
- 구현 중 부수 발견: `build_door_side.py`가 원래 `stop_type='stop'`만 처리해(종착 `stop_type='destination'`은 `dep_sec`가 없어 필터에서 제외) **전 노선 통틀어 종착역 문방향이 한 번도 계산된 적이 없었다** — 의정부·광운대·온수·수락산 등 "종착 여부"로 갈리는 조합을 풀려면 이 커버리지 확장이 필수라 계획 범위를 넓혀 반영(종착은 `arr_sec`만 있으면 유효, 대피 판정 대상에서는 제외).
- `scripts/build_door_side.py`에 `MULTI_ROW_RULES` 딕셔너리(조합별 판정 함수, 재사용 헬퍼 `_by_line_name`/`_by_train_no_prefix`/`_by_destination`/`_by_origin`/`_by_is_destination`/`_by_direction`)와 `norm_direction()`(door_directions 방향 텍스트를 trips.direction 'up'/'down'으로 정규화, 괄호 설명 붙은 값도 접두어로 처리) 신규 추가. `resolve_door()`가 방향 특정 키 → 방향 무관 키 순으로 조회해 Tier 1과 Tier 2를 하나의 경로로 통합.
- 재실행 결과: `stop_door_side` 543,706행 → **591,477행**(+47,771, 종착역 커버리지 확대 + 다중행 34개 해소분), 대피 316행 그대로(회귀 없음). `scripts/test_door_side_multi_row.py` 신규 작성(수원 Tier1·병점·구로·새절·의정부 종착·9호선 완행급행·스킵 확인 9건+부수검증 2건) 전부 통과, 기존 `test_door_side_known_cases.py`도 회귀 없이 통과, `PRAGMA integrity_check` ok.
- `README.md` §5(Tier A/B 설명에 종착역 커버리지·다중행 34개 해소·잔여 7개 사유 반영, 카운트 테이블 갱신), `todo.md`(다중행 항목을 잔여 4가지 유형으로 축소) 정리.

## 2026-08-31 — Claude (다중행 역 잔여 4건 사용자 확인으로 해소 + 진짜 버그 3건 추가 발견·수정)

- 전날 스킵했던 잔여 7개(중 4개 좁혀 재질문) 중 3개를 사용자가 직접 확인해줘 반영: (1) 지상서울은 문방향이 일반 서울역과 같은 오른쪽이라 "경부급행B 구분 필요 없음"이라는 취지였으나, 안양·의왕·군포·금천구청 4역 자체의 급행A/B 구분 신호는 여전히 없어 이 4개는 그대로 스킵 유지(재확인 필요 사항으로 todo.md에 남김). (2) 광운대 하행 '일반'(비종착)은 오른쪽 — 원본 셀 값(오른쪽, type 라벨만 없던 행)이 그대로 정답이었음, 종착 쪽만 여전히 '확인필요'라 스킵. (3) 경의중앙선/경의선 행신·강매는 "서울역행 경의선 열차"(line_name='경의선' & destination='서울', 47건 확인)만 서울역 착발(왼쪽), 나머지 전부 오른쪽으로 확정. 2호선 신도림·성수는 순환 방향 라벨(외선순환/내선순환/까치산발/신설동발)이 `trips.direction`('outer'/'inner'/'up'/'down')과 형식이 안 맞아 지금까지 매칭 자체가 안 되고 있었음을 확인·해소(종착 여부 기준, `stop_type='origin'`인 지선 시발역 쪽은 프로젝트 전체가 시발역 문방향을 계산 안 하는 기존 범위 밖이라 그대로 둠).
- 반영 전 원래 41개 "coarse" 목록(방향 무시, `line_sheet`+`group_id`만)을 재검토하다가 전날 미처 못 살펴본 9개(노량진·외대앞·동두천·안산·김포공항×2·검암·대구·중앙로·박촌)를 발견 — 대부분은 `direction`이 상행/하행으로 깔끔히 갈려 Tier 1으로 이미 정상 해석되고 있었지만, **노량진·동두천·안산 3곳은 실제 활성 버그였음**을 발견: 이 세 역은 "방향 무관 기본행(`direction=None`, 예: 노량진의 '일반')"과 "서비스/종착 한정 예외행(`direction`='상행'/'하행', 예: 노량진의 '경인급행')"이 공존하는데, 기존 `resolve_door()`가 방향이 일치하면 무조건 그 예외행을 채택해버려 **경인급행이 아닌 일반 열차까지 경인급행의 문방향을 잘못 받고 있었다**(노량진), 동두천은 종착 전용 '확인필요' 문자열이 상행 전체에 잘못 적용되고 있었고, 안산은 하행 종착값(오른쪽)이 하행 전체(원래 대피 가능한 왼쪽이어야 할 일반 정차분 포함)에 적용되고 있었다.
- `resolve_door()`를 `MULTI_ROW_RULES`가 `single_door`보다 항상 먼저 판단하도록 재설계(규칙이 `None`을 반환하면 다음 방향 키로 넘어가는 구조)해 노량진(line_name 기반)·동두천(종착 여부, 확인필요는 계속 스킵)·안산(종착 여부)을 바로잡고, 김포공항(공항철도)·검암·대구(대경선)·중앙로(대전1호선)도 같은 메커니즘으로 새로 해소(방향 텍스트가 목적지명/방면 문구라 형식이 안 맞던 것들). 박촌(인천1호선)도 규칙은 추가했으나 해당 역에 종착 사례 자체가 없어 사실상 미발동.
- 반영 중 별도의 큰 사전 발견: **`stop_type='stop'`인데 노선 전체가 `arr_sec`(또는 `dep_sec`) 하나만 저장하는 노선이 36개**(KTX·일반열차류는 원래 그렇고 Tier B라 무관하지만, 인천1호선·대전1호선·신분당선·김포골드라인·신림선·우이신설선·의정부경전철·용인경전철·GTX-A 등 `door_directions`가 있는 전동열차도 다수 포함). `build_door_side.py`의 기존 유효성 검사(`arr_sec AND dep_sec` 둘 다 필수)가 이 노선들의 모든 'stop'행을 걸러내 **중앙로(대전1호선)·박촌(인천1호선) 규칙은 코드상 맞지만 실제로는 한 번도 발동 못 함**을 직접 확인(종착행만 arr_sec가 있어 그쪽만 정상 동작). 오늘 범위 밖이라 `todo.md`에 별도 항목으로 등록(완화 방향: `stop_type='stop'`이면 arr_sec 또는 dep_sec 중 하나만 있어도 유효로 처리).
- `scripts/test_door_side_multi_row.py`에 이번 수정 12건(노량진 버그 수정 3건, 동두천 1건, 안산 2건, 대구·김포공항·검암 각 2건) 추가, 기존 것과 합쳐 30건 + 부수 검증 2건 전부 통과. 기존 `test_door_side_known_cases.py` 회귀 없음, `PRAGMA integrity_check` ok.
- 최종 결과: `stop_door_side` 591,477행 → **596,312행**(대피 316→399건 — 노량진/동두천/안산 버그 수정으로 되찾은 evac 케이스 다수 포함), 다중행 41개 조합 중 **37개 해소**(잔여 4개: 안양·의왕·군포·금천구청 경부급행B). `README.md`·`todo.md` 갱신.
