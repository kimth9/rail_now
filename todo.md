# todo

> 단기 작업 목록 — "이번 주 안에 처리할 항목". 장기 구상은 future_plan.md로.
> 완료한 항목은 지우고, 완료 사실은 history.md에 기록한다.

## 대기

### 데이터 정합성 (우선)

- [ ] 공휴일 `calendar_dates` 예외 테이블 추가 — 현재 `휴일` 다이어가 토·일에만 적용되어 설날·추석·광복절 등에 평일 시각표를 보여준다. 공휴일 목록은 공공데이터포털 특일 정보 API로 받는 편이 대체공휴일까지 커버되어 안전
- [ ] §8 검증 항목 스크립트화 — 지금은 수동 확인이라, 개정판에서 파서가 깨지면 조용히 틀린 데이터가 생성된다. 단조 증가·소요시간 범위·편수 급변·역 집합 차집합·미확인 매핑 잔존을 자동 체크
- [ ] **노선 추가·개정 체크리스트에 "타 도시 동명역 오병합 확인" 단계 상시 추가** — 대구/대전/부산 반영 때 총 13건 발견(2026-08-26). `merge_key` 메커니즘으로 해결(상세: `README.md` §4.2, `build_db.py`의 `meta.station_merge_key`). 앞으로 새 노선 추가나 다이어 개정 반영 시마다 빌드 후 "복수 노선 역사" 목록을 전수 점검해 무관한 동명역은 서로 다른 merge_key로, 실제 환승역은 같은 merge_key로 분리/병합할 것
- [ ] **KRIC 도시철도 운행시각표 API(`subwayTimetable`)로 arr_sec 부실 노선 보완 — 키 발급 요청 중(2026-08-31).** 정적 시각표가 열차별·역별 도착·출발시각을 모두 제공하는 노선은 그대로 쓰고, 그렇지 못한 노선(인천1호선·대전1호선·신림선 등 "역당 시각 1개" 구조 36개 중 일부)은 이 API로 파싱해와서 보완하는 방향(사용자 방침, 2026-08-31). 엔드포인트·파라미터·역 코드 매핑(`RAW/운영기관_역사_코드정보_2026.07.11_일반.xlsx`)은 세션 메모리 `reference_kric_subway_timetable_api` 참조. 키 발급되면: (1) 실제 응답 품질(커버리지·정확도) 검증, (2) 노선별로 정적 시각표 유지 vs API 전환 여부 판단, (3) 반영 방식 설계 순으로 진행

### 배포 판단

- [x] ~~DB 크기 100MB 초과 — 압축·분할 배포 판단 필요~~ → **최초 실행 시 다운로드 방식으로 확정(2026-08-31)**, 앱에 번들 안 함. 아래 항목으로 대체.
- [x] ~~GitHub 저장소 생성 + Release에 DB 업로드 + `DB_DOWNLOAD_URL` 채우기~~ → 완료(2026-08-31). 저장소 https://github.com/kimth9/rail_now (public), 릴리스 태그 `db-2026-08-31`에 `kr_rail_timetable.sqlite`(184MB) 업로드, `android/secrets.properties`(로컬, gitignore 대상)에 URL 채움 — 인증 없이 다운로드 가능함을 curl로 확인. DB 개정 시 새 태그로 릴리스만 추가하고 URL 갱신하면 됨.

### 출입문 방향 데이터 (2026-08-30 추가, 일부 완료)

- [ ] **`door_directions`의 "확인필요" 10개 역 — 승강장이 여러 개라 사용자 직접 확인 필요(2026-08-31 확정된 패턴, 단순 미조사 아님).** 1호선 광운대 하행 종착·동두천 상행 종착·서동탄·인천, 5호선 방화, 경의중앙선 문산, 대전1호선 반석·판암, 부산2호선 양산중앙, 동해선 부전(시종착). 코드(`build_door_side.py`의 `VALID_SIDES` 필터)가 이 값들을 실제 문방향으로 잘못 쓰지 않도록 전부 걸러 스킵하게 이미 수정해둬서(2,408건 오염 버그 수정 완료, 상세는 `history.md`) 데이터 오염 위험은 없음 — 남은 건 원본 확정뿐. (참고: "미개통" 2곳(GTX-A 삼성, 동해선 북울산)·"승강장없음" 1곳(1호선 마전)은 성격이 달라 이 목록에서 제외 — 각각 아직 개통 전 역, 여객취급 없는 신호장이라 애초에 판정 대상이 아님.)
- [ ] **Tier B 노선(대경선·동해선·GTX-A) 대피 판정** — KTX-이음/무궁화/ITX-마음/SRT가 통과역 행 자체를 안 남겨서 `build_door_side.py`의 시각 대조 방식이 안 통한다. 실시간 위치 추정이나 다른 방법 필요(대경선 서대구, 동해선 동래·송정·일광, GTX-A 성남·구성·동탄)
- [ ] 선로공유구간(경의중앙선/서해선 능곡~일산, 4호선/수인분당선 오이도~한대앞) 문방향 데이터를 `group_id` 단위로 통합 저장하는 방식 실제 구현 — 방침만 합의됐고 스키마·스크립트 반영은 아직 안 함

### 앱 (`android/`, 2026-08-31 뼈대 구축)

- [ ] 9화면 스텁(`ui/screens/`)에 실제 목업(`.dc.html`) 레이아웃·데이터 바인딩 채우기 — 지금은 전환 확인용 placeholder뿐
- [ ] `data/network/` Retrofit 스텁(KRIC·서울시)에 실제 호출부·에러 처리 연결
- [ ] Room DB 스모크 테스트(`app/src/androidTest/.../RailNowDatabaseSmokeTest.kt`)는 Robolectric이 API 36을 아직 지원하지 않아 `androidTest`(실기기·에뮬레이터 전용)에 있음 — Robolectric이 36을 지원하면 JVM 유닛테스트로 옮기는 걸 재검토

### 확인 필요

- [ ] **서울시 실시간 위치정보 API — 첫차 운행 시작(오전 5시 반 이후) 뒤 실데이터 응답 확인만 남음.** 2026-08-31 새벽 무한로딩 문제는 **URL 자체가 틀렸던 것**이 원인으로 확정(잘못된 URL: `http://openapi.seoul.go.kr:8088/...` → 올바른 URL: `http://swopenapi.seoul.go.kr/api/subway/{키}/json/{서비스}/{시작}/{끝}/{역명|호선명}`, 사용자가 직접 찾아줌). 올바른 URL로 재시도한 결과 **키 2개(`SEOUL_SUBWAY_KEY`/`SEOUL_SUBWAY_KEY2`) × API 2개(`realtimeStationArrival`/`realtimePosition`) = 4개 조합 전부 인증 정상**(틀린 키로 받는 `INFO-100` 에러와 구분 확인함) — 도착정보는 실제 데이터까지 받음, 위치정보는 심야라 "해당하는 데이터가 없습니다"(`INFO-200`, 인증은 통과)만 뜨는 상태. 서비스 시간대에 위치정보도 실데이터 나오는지만 확인하면 완전히 종료

