# todo

> 단기 작업 목록 — "이번 주 안에 처리할 항목". 장기 구상은 future_plan.md로.
> 완료한 항목은 지우고, 완료 사실은 history.md에 기록한다.

## 진행 중

- [ ] 없음

## 대기

### 데이터 정합성 (우선)

- [ ] 공휴일 `calendar_dates` 예외 테이블 추가 — 현재 `휴일` 다이어가 토·일에만 적용되어 설날·추석·광복절 등에 평일 시각표를 보여준다. 공휴일 목록은 공공데이터포털 특일 정보 API로 받는 편이 대체공휴일까지 커버되어 안전
- [ ] §8 검증 항목 스크립트화 — 지금은 수동 확인이라, 개정판에서 파서가 깨지면 조용히 틀린 데이터가 생성된다. 단조 증가·소요시간 범위·편수 급변·역 집합 차집합·미확인 매핑 잔존을 자동 체크
- [ ] **노선 추가 체크리스트에 "타 도시 동명역 오병합 확인" 단계 상시 추가** — 대구/대전 반영 때 처음 발견(2026-08-26): `build_db.py`의 `add()`는 순수 이름 매칭이라, 서로 무관한 도시의 노선망을 섞기 시작하면(용산·중앙로·시청·대공원·구암 등 흔한 지명) 실제 DB를 빌드해봐야 오병합이 드러난다. 앞으로 부산/서울교통공사/인천 등 새 노선 추가 시마다 빌드 후 "복수 노선 역사" 목록을 전수 점검해 무관한 동명역을 `CITY_DISAMBIGUATE`류 딕셔너리로 분리할 것

### 확인 필요

- [ ] 코레일 `travelerTrainRunPlan2` 검증 — KTX 파일에 없는 중간역 도착시각과 운전정차를 보완할 수 있는지. `japanese_style_timetable/scripts/fetch_station_data.py`에 호출 코드가 이미 있으나 환경변수 이름 불일치(`TRAIN_TIME_API_KEY_3/4` vs `.env`의 `TAGO_API_KEY_*`)로 실행 불가 상태 — 이름만 맞추면 반나절이면 판가름
- [ ] 프로젝트명 확정 — 현재 `kr-rail-timetable`은 임시
- [ ] 앱 스택 확정 — README 상태줄의 Kotlin/Compose + Room은 가정값
- [ ] 일반열차 원본 데이터 이상치 원본 재확인 — 호남선 1491/1492(종착 후 잔재 시각), 장항선 1246(종착역 라벨 자체가 틀림, 실제로는 홍성 이후 평택까지 정상 운행). 코레일에 다음 개정판이 나오면 고쳐졌는지부터 확인, 안 고쳐졌으면 수동 보정표 필요

### 노선 추가

`RAW/korail_시각표_20260825/`에 코레일 노선별 최신 시각표 17개(심야임시전동열차 제외) 확보됨. 전체 계획: `C:\Users\User\.claude\plans\ethereal-herding-crown.md` — 한 번에 1개씩 파싱 → 파서 작성 반복.

- [x] 서해선(전동, 대곡~일산 직결) 반영 (2026-08-25) — `scripts/parse_seohae.py`(신규, `parse_gyeongui.py` 재사용). 원본에 문산 방면(탄현~판문) 12개 역이 빈 꼬리 행으로 붙어있어 시각 데이터 없는 역은 등록에서 제외. 역명 21개 중 9개가 "신" 접두사 패턴(신초지→초지, 신소사→소사 등), 1개(신김포→김포공항)만 "신"+절삭이 겹친 이례적 형태라 나무위키 역 목록 위치 대조로 확정. 일반열차 파일의 서화성~홍성 서해선과 구분하려 `line_name`을 `서해선(전동)`으로 등록. 소사(1호선)·초지(수인분당선·안산과천선)·대곡/능곡/곡산/백마/풍산/일산(경의중앙선·경의선)이 station_groups에서 정상 병합됨

- [x] 수인분당선 반영 (2026-08-25) — `scripts/parse_suin_bundang.py`. 역명 63개 전부 나무위키 대조로 확정. `신인천`/`신수원`은 절삭이 아니라 코레일 전산 표기임을 확인, `인천`/`수원`으로 정규화해 1호선측과 station_groups에서 정상 병합 확인(인천·수원·청량리·수서)
- [x] 안산과천선(4호선 진접~오이도) 반영 (2026-08-25) — `scripts/parse_ansan_gwacheon.py`. 역명 51개 전부 나무위키 대조로 확정. `당고개`(2024년 불암산으로 개칭)·`4동운`(2009년 개칭 전 동대문운동장 흔적) 등 원본의 개칭 미반영 사례 발견. `시흥기`는 정거장수 대조로 비정차 차량기지(시흥기지)임을 확인해 JUNCTIONS 분리. station_groups에서 서울·금정·동대문·창동이 1호선과, 한대앞~오이도 구간이 수인분당선과 정확히 병합됨을 확인(4호선-수인분당선 선로공유 구간과 일치)
- [x] 경의중앙선(문산~지평) 반영 (2026-08-25) — `scripts/parse_gyeongui_jungang.py`. 역명 53개 전부 나무위키·위키백과 대조로 확정. `1양정`/`1양원`이 한국철도물류정보시스템 내부 등록명(위키백과 "양정역(남양주)" 문서로 확인)임을 발견 — 신인천/신수원과 같은 부류지만 다른 노선. station_groups에서 청량리(4-way)·왕십리·이촌·행신·상봉·덕소·양평 등이 기존 노선과 정확히 병합됨을 확인
- [x] 경의선(도라산~서울) 반영 (2026-08-25) — `scripts/parse_gyeongui.py`. 경의중앙선과 헤더·연계열번 구조가 완전히 같아 파서를 거의 그대로 재사용. 경의중앙선과는 가좌에서 갈라지는 별개 운행계통(신촌 경유 vs 용산선 경유)임을 확인, 가좌~문산 19개 역이 station_groups에서 정확히 공유 병합됨. `도라산`은 정차 기록 0건으로 나무위키의 "운행 중단" 서술과 일치
- [x] 경춘선(청량리~춘천) 반영 (2026-08-25) — `scripts/parse_gyeongchun.py`. 역명 25개 전부 나무위키 대조로 확정(`평내호`→`평내호평` 1건만 절삭). `광운대시종착(평일)` 시트가 한 시트 안에 상행·하행 두 블록이 나란히 있는 특이 구조라 블록별 역명 열/데이터 열 범위를 지정해서 처리 — 나무위키의 "춘천발 광운대행 평일 2회"와 파싱 결과(상행2·하행2)가 정확히 일치
- [x] 경인급행 갱신 (2026-08-25) — `scripts/parse_gyeongin_express.py`. 새 노선이 아니라 1호선에 이미 있던 경인급행 서비스의 최신 개정판(5.26 vs 1호선 원본 2.28)임을 발견해 사용자 확인 후 교체 방식으로 진행. `parse_line1.py`의 SKIP_SHEETS에 옛 경인급행 시트 4개 추가, `build_db.py`에서 별도 노선이 아니라 LINE_OF["LINE1"]로 등록해 기존 1호선 역과 stop_id 공유. 옛 편수(367)와 새 편수(367)가 정확히 일치해 순수 교체임을 검증
- [x] 경강선(판교~여주) 반영 (2026-08-25) — `scripts/parse_gyeonggang.py`. 역명 12개 전부 나무위키 대조로 확정. `신이매`/`신판교`가 신인천/신수원과 같은 부류의 "코레일 내부 동명역 구분 접두사"임을 재확인. station_groups에서 이매(수인분당선)·부발(국철 중부내륙선)과 정확히 병합됨
- [x] 동해선(전동, 부전~태화강) 반영 (2026-08-25) — `scripts/parse_donghae.py`. 역명 23개 전부 나무위키 대조로 확정. 계획 문서에서 미리 경고했던 "동해선 노선명 충돌"이 실제로 발생 — KTX·일반열차 파일에도 별개의 "동해선"이 있어 `line_name`을 `동해선(전동)`으로 구분해 등록. 부전·태화강·남창·기장·신해운대·센텀이 KTX(국철) 동해선 시트와 station_groups에서 정상 병합됨 확인
- [x] 대경선(구미~경산) 반영 (2026-08-25) — `scripts/parse_daegyeong.py`. 역명 14개 전부 실명(절삭 없음) — 이 노선은 이름이 아니라 구성이 함정이었다: 원본 라벨 14개 중 8개만 공식 정차역이고 6개(가천·고모·지천·신동·연화·약목)는 열차가 통과만 하는 실재 역. JUNCTIONS로 빼지 않고 일반 역 등록으로 충분(기존 stop_type 로직이 자동으로 'pass' 처리). 서대구·동대구·경산이 KTX(국철)와 station_groups에서 정상 병합됨
- [x] 일산선(3호선 오금~대화) 반영 (2026-08-25) — `scripts/parse_ilsan.py`. **전동열차 노선 전부 반영 완료(계획 문서 3단계 종료).** 역명 44개 전부 나무위키 대조로 확정. `3`으로 시작/끝나는 라벨 8건이 3호선 구분 표기(안산과천선 `4` 접두사와 같은 부류)임을 확인 — `3`이 접두사·접미사 양쪽 다 올 수 있어 위치만으로 일반화 금지. `동대입`은 절삭이 아니라 정식 역명(동대입구)이었음. station_groups에서 수서·종로3가·도곡·충무로·옥수·대곡·교대가 전부 예상대로 병합 — 특히 `교대` 병합으로 동해선의 `부교대` 접두사 이유가 최종 확인됨
- [x] 1호선 갱신 (2026-08-25) — 새 원본 파일로 재실행, 기존 파서 무수정으로 동작(840/729편, 경고 이전과 동일). `output/kr_rail_timetable.sqlite`에 반영됨
- [x] KTX 갱신 (2026-08-25) — 새 원본 파일로 재실행, 기존 파서 무수정으로 동작(621편, 경고 0). `output/kr_rail_timetable.sqlite`에 반영됨
- [x] ITX-청춘(용산~춘천) 반영 (2026-08-25) — `scripts/parse_itx_chuncheon.py`. **계획 문서(전동열차+ITX-청춘) 전부 완료.** 경의중앙선·경춘선을 그대로 잇는 열차라 역명 31개가 이미 그 두 노선에서 검증된 것들 — 새 조사 불필요. `build_db.py` 결과 새 station_groups 0개(전부 기존 그룹에 병합)로 노선 간 역명 일관성 재확인
- [x] 일반열차(15개 노선) 반영 (2026-08-25) — `scripts/parse_ilban.py`(신규, `parse_ktx.py`를 참고했지만 레이아웃이 달라 재작성). 역=행/열차=열 전치 구조인데 역당 시각 1개(KTX 방식)인 하이브리드 레이아웃. 실제 데이터 열 뒤에 한자·영문 라벨 열이 붙어있어 열차번호=숫자 필터로 해결. 원본 자체의 데이터 이상치(찌꺼기 시각, 잘못된 종착역 라벨) 2건 발견, 6시간 임계값으로 자정 넘김 오판만 방지. 노선명 충돌 방지로 전부 `(일반)` 접미사, stops는 KTX와 같은 "국철"로 병합
- [x] 관광열차(5개 서비스 14개 편성) 반영 (2026-08-25) — `scripts/parse_gwangwang.py`(신규). 가장 비정형인 레이아웃 — "역명" 셀 마커 스캔으로 좌표 하드코딩 없이 블록 전부 탐지. 블록 사이 빈 행이 없는 경우(V-트레인 연계편)를 처음엔 놓쳐서 다음 블록 제목까지 역명으로 오독했다가 종료조건 수정으로 해결. stops는 국철로 병합, 새 역 0개
- [x] **코레일 다운로드 게시판에서 확보한 17개 시각표(그룹 A/B/C) 전부 반영 완료 (2026-08-25).** 계획 문서(`~/.claude/plans/ethereal-herding-crown.md`) 전 항목 종료
- [ ] 노선 추가하며 DB 크기 증가 곡선 계속 확인 — 100MB 초과 시 압축·분할 배포 판단 (현재 47.8MB, 대구1/2/3호선+대전1호선까지 반영 완료 상태)
- [x] 대구 도시철도 1/2/3호선(대구교통공사) 반영 (2026-08-26) — `scripts/parse_daegu.py`(신규, 3개 파일 공용). 헤더 2행(운용번호/열차번호), 역=2행1조 전치구조 — 시각이 datetime이 아니라 'HH:MM:SS' 텍스트로 저장돼 있어 별도 파싱 필요했음. 평일/**토요일**/휴일 3종 다이어 최초 확인(`todo.md` 기존 "토요일 다이어 존재 확인" 항목의 답이 됨). 명덕1/반월당1/반월당2/청라언덕2/청라언덕3/명덕3처럼 동명역에 자기 호선 번호가 접미사로 붙는 패턴을 3개 파일 상호 대조로 확정(나무위키 불요, 역명·개수 전부 실제 노선도와 일치 확인). **build_db.py 자동 병합이 이름만으로 작동한다는 점 때문에 실제 DB 빌드 후 오병합 9건 발견**(용산·신기·대곡·죽전·대공원·교대·신천·중앙로·구암 — 대구/대전이 서울·수도권·서로와 무관한 동명역을 갖고 있었음) → `CITY_DISAMBIGUATE` 딕셔너리로 수정, 반대로 대구역/동대구역은 "역" 접미사 때문에 대경선과 병합이 안 되고 있던 것도 정규화해 고침(둘 다 실제 도보환승역, 나무위키 확인). 단조증가 검증 통과(914+844+888 trip, 위반 0건)
- [x] 대전 도시철도 1호선(대전교통공사) 반영 (2026-08-26) — `scripts/parse_daejeon.py`(신규). "출발역 기준 배차표"(열차=열 매트릭스가 아니라 역마다 발차시각 목록만 있는 레이아웃) — 열차 단위 컬럼이 없어 인접 역 시각을 FIFO 그리디 매칭(최대 간격 6분)으로 이어붙여 trip을 복원하는 별도 알고리즘 필요했음(실제 관측시각 기반, 배차간격 추정과는 다름). 종착역(판암/반석) 자체는 출발 블록이 없어 시각이 없는데 이건 마지막 관측역+통상 간격(2분) 근사치로 보정(캐비어트로 명시). 대전역은 코레일 대전역과 지하로 연결된 실제 환승역(위키 확인)이라 자동 병합, 용문·시청·중앙로·구암은 수도권·대구와 동명이라 `CITY_DISAMBIGUATE`로 분리. 단조증가 위반 0건(460 trip)
- [ ] **다음: 부산 1~4호선 + 부산김해경전철** — 계획 문서(`~/.claude/plans/bubbly-meandering-pudding.md`) 2단계. RAW 확보됨(부산1~4호선 통합 1파일 2026-08-24 최신, 부산김해경전철 2022-11-21). 시트 구성 미조사, 착수 시 구조부터 확인 필요
- [ ] 서울교통공사 2·3·5~9호선 반영 — 계획 문서 3단계. RAW 전부 확보됨(2026-08-26 재확인). **평일/휴일판 기준일 격차 존재**(2·7호선이 가장 심각: 평일 2026-08-21 vs 휴일 2022-04/06, 4년+ 차이 — 사용자 결정: RAW 그대로 진행하고 README에 캐비어트만 명시, 재확보는 후속 과제). 8호선은 휴일 후보가 2개(231007 구버전, 240810 "주말최신") — 파싱 시 내용 대조로 확정 필요. 파일 구성이 복잡(2+7호선 합본 1개, 5~8호선 합본 1개, 개별 파일 혼재)이라 노선별이 아니라 파일 단위로 먼저 구조 확인 필요
- [ ] 인천1호선(.xls 구버전 바이너리 — openpyxl 단독으로 안 열리면 xlrd 추가 필요) + 공항철도(.xlsx, 평일/휴일 구분 시트 내부 확인 필요) 반영
- [ ] 웹크롤링 대상 8곳 반영 — RAW 파일 없음, 각 사이트 방문해 페이지 구조부터 조사 후 전용 크롤러 작성: 신림선(sillimlrt.com), 우이신설선(ui-line.com), 김포골드라인(gimpogoldline.com), GTX-A 운정중앙~서울/수서~동탄(gtx-a.com, lineCd=L08/L09), 광주1호선 소태·녹동 방향(grtc.co.kr, 원본 미확보 확인용)
- [ ] 추정 필요 대상 2곳 반영 — 인천2호선(ictr.or.kr), 용인경전철(ever-line.co.kr). 공식 시각표 없이 배차간격만 공개 — 역간 소요시간 추가 조사해 간이 시각표 직접 생성 필요, README에 "추정치 기반" 캐비어트 필수
- [ ] 사진 기반 대상 1곳 반영 — 의정부경전철(ulrt.co.kr, 시각표가 이미지로만 제공) — Claude가 이미지를 직접 읽어 육안 전사
- [ ] 신분당선 반영 — RAW에 xlsx 없이 PDF(평일, 2025-01)+PNG(휴일, 2022-05)만 있음. PDF는 표 추출 라이브러리 시도 후 실패 시 육안 전사, PNG는 육안 전사. 평일/휴일 기준일도 2.5년 이상 격차. 계획 문서 8단계(안정적 방식 전부 끝난 뒤 마지막 순서)
- [ ] 광주1호선 원본 확보 여부 재확인 필요(2026-08-26 재조사에서도 RAW에 없음 재확인) — 위 크롤링 대상에 grtc.co.kr 방향별 URL이 있으니 그걸로 대체 가능한지 그때 판단

### 위생

- [ ] `japanese_style_timetable`의 `.env` 키 3개 로테이션 — zip 압축 시 평문 포함된 채로 외부에 전달된 이력 있음
