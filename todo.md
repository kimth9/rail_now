# todo

> 단기 작업 목록 — "이번 주 안에 처리할 항목". 장기 구상은 future_plan.md로.
> 완료한 항목은 지우고, 완료 사실은 history.md에 기록한다.

## 대기

### 데이터 정합성 (우선)

- [ ] 공휴일 `calendar_dates` 예외 테이블 추가 — 현재 `휴일` 다이어가 토·일에만 적용되어 설날·추석·광복절 등에 평일 시각표를 보여준다. 공휴일 목록은 공공데이터포털 특일 정보 API로 받는 편이 대체공휴일까지 커버되어 안전
- [ ] §8 검증 항목 스크립트화 — 지금은 수동 확인이라, 개정판에서 파서가 깨지면 조용히 틀린 데이터가 생성된다. 단조 증가·소요시간 범위·편수 급변·역 집합 차집합·미확인 매핑 잔존을 자동 체크
- [ ] **노선 추가·개정 체크리스트에 "타 도시 동명역 오병합 확인" 단계 상시 추가** — 대구/대전/부산 반영 때 총 13건 발견(2026-08-26). `merge_key` 메커니즘으로 해결(상세: `README.md` §4.2, `build_db.py`의 `meta.station_merge_key`). 앞으로 새 노선 추가나 다이어 개정 반영 시마다 빌드 후 "복수 노선 역사" 목록을 전수 점검해 무관한 동명역은 서로 다른 merge_key로, 실제 환승역은 같은 merge_key로 분리/병합할 것

### 배포 판단

- [ ] **DB 크기 100MB 초과(2026-08-27 기준 113.4MB) — 압축·분할 배포 판단 필요.** 계획 문서 로드맵(27개 노선, 웹크롤링 8곳·추정치 2곳·사진 전사 1곳·PDF+PNG 1곳)은 전체 완료된 상태. 크기 추이는 history.md 각 반영 항목 참조. 사용자 판단 필요(예: 계통별 분할 DB, 압축 배포, 서버 다운로드 방식 전환 등)

### 출입문 방향 데이터 (2026-08-30 추가, 일부 완료)

- [ ] **`door_directions`에 원본이 아직 못 채운 placeholder 값('확인필요'·'미판정'·'미개통'·'승강장없음')이 남아있는 역 재확인** — 2026-08-31, 이 값들이 실제 문방향인 것처럼 `stop_door_side`에 그대로 새고 있던 버그(2,408건)를 발견해 수정(`VALID_SIDES` 필터 + `SKIP` sentinel 도입, 상세는 `history.md`). 코드는 이제 이 값들을 전부 걸러 스킵하므로 데이터 오염 위험은 없어졌고, 남은 건 원본 확정뿐. '확인필요'는 단순 미조사가 아니라 **승강장이 여러 개라 사용자가 직접 확인해야 하는 패턴**(사용자 확인, 2026-08-31) — 1호선 광운대 하행 종착·동두천 상행 종착, 동해선 부전(시종착) 3곳.
- [ ] **중간역 문방향 미계산 — 원인 확정(2026-08-31 재점검), 수정만 남음.** `build_door_side.py`가 `stop_type='stop'`행에 `arr_sec`·`dep_sec` 둘 다 요구하는데, 36개 노선(KTX·일반열차 14개 계열 + 인천1호선·대전1호선·신분당선·김포골드라인·신림선·우이신설선·의정부경전철·용인경전철·GTX-A 등 전동열차 다수)은 원본 자체가 "역당 시각 1개"(출발역 기준 배차표 또는 KTX식 단일 셀) 구조라 `arr_sec`가 원천적으로 없다 — 9개 파서(`parse_incheon1.py`·`parse_gtxa.py`·`parse_sillim.py`·`parse_ui_sinseol.py`·`parse_uijeongbu.py`·`parse_yongin.py`·`parse_sinbundang.py`·`parse_gimpo_goldline.py`·`parse_gwangju1.py`) 전부 자체 docstring에 이 구조를 명시하고 있어 **파서 버그가 아니라 원본 데이터 한계**임을 확인함. 문방향(`normal_side`) 자체는 시각 구간 없이 고정값이라 `dep_sec` 하나만으로도 판정 가능 — `build_door_side.py`의 유효성 검사를 "`stop_type='stop'`이면 `arr_sec` 또는 `dep_sec` 중 하나만 있어도 유효"로 완화하면 될 것으로 보이나, 대피(Tier A) 판정부(`r["arr_sec"] <= pass_time <= r["dep_sec"]`)는 `arr_sec`가 `None`이면 비교 시 `TypeError`가 나므로 별도 가드(대피 판정은 arr_sec 있을 때만 시도, 없으면 무조건 평시값) 필요.
- [ ] **Tier B 노선(대경선·동해선·GTX-A) 대피 판정** — KTX-이음/무궁화/ITX-마음/SRT가 통과역 행 자체를 안 남겨서 `build_door_side.py`의 시각 대조 방식이 안 통한다. 실시간 위치 추정이나 다른 방법 필요(대경선 서대구, 동해선 동래·송정·일광, GTX-A 성남·구성·동탄)
- [ ] 무인경전철 4개 노선(김포골드라인·신림선·의정부경전철·용인경전철) 문방향 값 재검증 — 나무위키 직접서술을 못 찾아 "통행방향×승강장형태" 역산으로만 채워서 신뢰도가 낮음(상세: `docs/analysis/260830_1120_출입문방향조사_신규8노선.md`)
- [ ] 선로공유구간(경의중앙선/서해선 능곡~일산, 4호선/수인분당선 오이도~한대앞) 문방향 데이터를 `group_id` 단위로 통합 저장하는 방식 실제 구현 — 방침만 합의됐고 스키마·스크립트 반영은 아직 안 함

### 확인 필요

- [ ] 앱 스택 확정 — README 상태줄의 Kotlin/Compose + Room은 가정값

