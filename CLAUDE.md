# kr-rail-timetable — Claude 작업 가이드

## 1. 목표

한국 전체 철도(고속철도 + 광역전철) 시각표를 오프라인 SQLite DB로 구축하고, Android 16 Live Update 알림으로 열차 도착·출발을 추적하는 앱. 현재는 앱 착수 전, 데이터 파이프라인(엑셀 → SQLite) 구축 단계.

## 2. 폴더 구조·핵심 모듈

- `scripts/parse_ktx.py` — KTX 시각표 파서 (열차=행 / 역=열)
- `scripts/parse_line1.py` — 1호선 파서 (역=행 / 열차=열, 전치 구조). 역명 3글자 절삭 복원 매핑(`STATION_ALIAS`) 포함
- `scripts/build_db.py` — 파서 출력 CSV들을 하나의 SQLite로 병합
- `data/line1_stops.csv` — 1호선 역명 절삭 복원 매핑 검수용
- `output/kr_rail_timetable.sqlite` — 배포용 DB 번들 (현재 195 stops / 2,190 trips / 65,363 stop_times, 10.6MB)
- 원본 엑셀(KTX·1호선 시각표)은 `RAW/`에 넣고 절대 수정하지 않는다.

노선마다 원본 엑셀 레이아웃이 완전히 달라 파서를 노선 계열별로 분리해 둔 것 — 하나로 합치면 더 취약해진다. 출력 스키마만 맞춰 `build_db.py`에서 합류. 상세 스키마·파싱 함정·검증 항목은 `README.md` §5~8 참조.

## 3. 실행 명령

```bash
python scripts/parse_ktx.py   <KTX시각표.xlsx>    out/
python scripts/parse_line1.py <1호선_평일.xlsx>   out_line1/
python scripts/build_db.py    output/kr_rail_timetable.sqlite
```

의존성: `openpyxl`뿐.

## 4. 도메인 핵심 지식

재발견 비용이 큰 파서 함정만 요약(전체는 `README.md` §6):
- KTX 원본의 미정차는 `00:00:00`이지 빈칸이 아니다. 같은 파일에 `00:03` 같은 실제 심야 도착도 있어 "0시대=미정차"로 처리하면 막차가 사라진다 — 정확히 `00:00:00`만 걸러야 한다.
- 1호선 원본은 역명이 3글자로 절삭돼 있다(`온양온`→온양온천). 접두사 `1`은 서울 지하철 1호선 지하구간 표기. 여기가 틀리면 환승 연결이 깨진다.
- `시흥연`은 역이 아니라 시흥연결선(분기점) — `junctions` 테이블로 분리.
- 역 모델은 `station_groups`(사용자 검색 단위) → `stops`(노선별 승강장) 2계층. 계통(경인·경부장항 등)은 나누지 않고 `trips.line_name` 태그로만 구분한다.

## 5. (해당 시) 이 프로젝트만의 예외·추가 규칙

<팀 공통 규칙은 워크스페이스 루트 `../CLAUDE.md`에서 자동 상속되므로 여기 재기술하지 않는다
(Claude Code가 cwd에서 상위 폴더로 올라가며 CLAUDE.md를 자동으로 합쳐 읽음).
이 프로젝트에만 해당하는 예외·추가 규칙이 있을 때만 근거·승인일과 함께 적는다. 없으면 이 섹션째로 삭제.>

- 목록·번호 표기 시 원문자 유니코드(①②③, ⒶⒷⒸ 등)를 쓰지 않고 괄호 표기(`(1)`, `(2)`, `(A)`, `(B)`)를 사용한다 — VS Code 폰트가 원문자를 제대로 렌더링하지 못함(전역 규칙, `~/.claude/CLAUDE.md`). 팀 표준은 공통 규칙의 프로젝트별 재기술을 원칙적으로 금지하지만, 전역 규칙만으로는 출력에 반영되지 않는 문제가 반복 확인되어(기존 24개 프로젝트에 개별 재기재, 2026-07-15) 신규 프로젝트도 처음부터 포함하도록 템플릿 자체에 반영한다 (2026-07-19).

## 6. 참고 문서

- `../프로젝트_표준_가이드.md` — 팀 폴더 표준
- `../japanese_style_timetable/` — 이 프로젝트 이전에 작업한 코레일 시각표 앱 프로젝트(형제 폴더). TAGO API 역 매핑(265개 역 ↔ 노드 ID), 열차 종별 폴백, 심야 시각 보정 등 재사용 가치가 있는 자산이 있다. 단, 정차역 상세를 rail.blue 개인 사이트 내부 API로 가져오는 구조라 그대로 이식하지 말고 공식 소스로 대체할 것 — 상세는 `history.md` 2026-08-25 "기존 프로젝트 자산 조사" 항목 참조.
- `docs/` — 프로젝트별 상세 문서
