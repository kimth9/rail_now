# future plan

> 장기 구상 — "당장 안 하지만 잊으면 안 되는 방향". 단기 할 일은 todo.md로.

## 구상

### 안드로이드 앱 착수

데이터 파이프라인이 어느 정도 서면 앱 골격 + Room 연동. 착수 시점의 판단 근거는 "시각표만으로 핵심 가치가 증명되는가"다.

**알림 로직을 가장 먼저 붙인다.** 현재 SQLite만으로 "출발 15분 전 알림"이 서버·실시간 API 없이 완전히 구현 가능하다. 오프라인에서도 알람이 울리는 것이 기존 웹 서비스(rail.blue 등)가 구조적으로 못 하는 지점이자 이 프로젝트의 존재 이유이므로, 이걸 가장 빨리 확인하는 것이 우선순위를 정하는 기준이 된다.

Stem-and-Leaf 역 시각표 화면(Center-Hour 3단 그리드)은 `japanese_style_timetable`에서 검증된 레이아웃을 Compose로 재구현.

### 시각표 번들 원격 갱신

다이어 개정 시 앱 업데이트 없이 시각표를 교체할 수 있어야 한다. 원격에 번들을 두고 `meta`의 버전만 확인해 받아오는 구조. 파일 하나 올려두는 정적 호스팅이면 충분해 비용은 거의 들지 않는다.

동기화가 실패해도 기존 로컬 데이터로 계속 동작해야 한다 — 지하 승강장에서 신호가 끊기는 상황이 흔하다.

### 실시간 오버레이

```
[베이스] 시각표   전 노선 · 오프라인 · 항상 존재
              ↓ 있으면 덮어씀
[오버레이] 실시간  일부 구간만
```

수도권부터. 서울 열린데이터광장 `realtimeStationArrival`이 표준 선택지이나 서울시 이외 역 구간이 빠지므로, 인증키 발급 후 경계 역(부평·정자·대화 등)을 직접 찍어 구멍 목록을 만들어야 한다. `recptnDt`와 현재 시각의 차이만큼 열차가 진행한 것으로 보정하는 로직이 필수이고, 진행바가 뒤로 튀지 않도록 클라이언트에서 단조 감소를 강제해야 한다.

이후 부산. 대구·대전·광주는 실시간 공개 API가 확인되지 않아 시각표 전용으로 남을 가능성이 높다 — 기관 직접 문의로 열릴 여지는 있다.

**갱신 주기가 10~30초라 WorkManager(최소 15분)로는 불가.** 포그라운드 서비스가 필요하고 Android 14+ FGS 타입 선언·정당화가 따른다. 추적 세션을 짧게(최대 30분, 도착 시 자동 종료) 제한하는 것이 배터리·정책 양쪽에 유리하다.

사용자가 늘면 클라이언트 직접 호출은 인증키 노출과 기관별 트래픽 한도에 걸린다. 중간 서버를 두고 역 단위로 캐싱하면 동일 역 사용자 N명이 호출 1건을 공유한다.

### 앱 위젯

상시 조회용 화면은 **알림이 아니라 위젯으로 제공**한다. Live Update 정책이 "앱 기능에 빠르게 접근하는 용도라면 위젯이나 퀵 설정 타일을 쓰라"고 명시하므로 알림으로 대체할 수 없다.

### 승강장 계층 및 환승 소요시간

현재 `stops`는 노선별 승강장, `station_groups`는 역사 단위다. 서울역 지하 1호선 ↔ 지상 KTX 도보 이동시간 같은 정보가 필요해지면 `station_groups`에 `transfer_min`을 얹는다. 환승 경로 안내를 하게 되는 시점의 과제다.

### 여객철도 여정 추적

승차 중인 열차를 ProgressStyle로 추적하는 기능. 정차역=`Point`, 역간=`Segment`로 매핑되어 리드셰어 유스케이스와 구조가 거의 같고, "사용자가 시작한·진행 중인·시간에 민감한" Live Update 조건을 광역전철 도착 알림보다 오히려 깔끔하게 충족한다. 다만 광역전철 실시간과 여객철도 시각표를 한 앱에서 다루면 UX가 산만해질 위험이 있어 화면 분리 설계가 선행되어야 한다.

### 데이터 공개

스키마를 GTFS 개념에 맞춰 두었으므로 GTFS 정식 변환이 어렵지 않다. 공개하면 도구 생태계를 쓸 수 있고, 운영사 공식 배포 자료만 사용하므로 출처 표시 조건도 충족 가능하다.

## 보류 (사유와 함께)

- **rail.blue 데이터 활용** — 철도 정보의 폭과 깊이는 가장 뛰어나지만 개인 운영 사이트의 축적 자산이다. 개별 프록시와 벌크 추출은 성격이 다르고, 앱이 성장할수록 부담이 커진다. **검증용 대조 소스로만** 참조한다. 운영자와 직접 협의가 되면 재검토
- **명절 임시열차** — 공식 배포 시각표에 아예 없다. 앱이 가장 많이 쓰일 시점에 정작 임시열차가 안 보이는 문제라 중요하나, 상시 확보 경로가 없어 실시간 오버레이 구축 이후로 미룬다
